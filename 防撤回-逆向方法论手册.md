# 飞书防撤回：从混淆海里捞出关键函数到落地成插件 —— 操作思路手册

> 这份手册记录的是**思路和方法论**，不是命令清单。用飞书防撤回这个真实案例当贯穿例子，
> 但每一步抽象出来的原则，对任何"在混淆/剥离符号的大型 App 里找一个关键点并改它"的逆向都通用。
> 工程落点见同目录 `xposed-antirecall\`，机制细节见记忆 `feishu-antirecall`。

---

## 0. 一句话总览

> **不要去逆"那个函数"，去逆"数据必经的那道关"。**
> 撤回最终一定要把"已撤回"这件事**写进本地库**——存储写入是绕不过去的咽喉，
> 它比任��混淆过的业务函数都稳定得多。找到它、看清它、最小化地改它。

---

## 1. 先把目标定准（决定你该在哪一层动手）

逆向最容易走偏的一步，是把目标定得太泛（"关掉撤回"）。先逼自己写出**精确的成功判据**：

- ❌ 模糊："让飞书不撤回消息"
- ✅ 精确："消息被撤回后，**原文仍能在本地看到**，**包括我没打开过的聊天**"

加上"未打开的聊天"这一条，立刻排除了一整类方案：

- 我们之前已验证：**未打开聊天的消息原文，从不进入 Java 层**（UI 层只在打开聊天时才把内容 materialize 到 Java）。
- ⇒ 任何 Java/Xposed 的 UI 层 hook，对"未打开聊天"天然够不到。这是**架构上限**，不是技巧问题。

**原则 1：目标里每多一个约束，就砍掉一批方案。先定判据，再选战场。**

---

## 2. 分层 → 找咽喉（choke point thinking）

把 App 拆成数据流经的层，问每一层："撤回这件事在这里留下什么痕迹？"

```
网络/推送  →  原生 rust 解析(liblark.so)  →  本地加密库(sqlcipher)  →  Java 模型  →  UI 渲染
   ↑收到撤回命令        ↑标记撤回/清空内容          ↑把"清空"持久化         ↑(仅打开聊天)   ↑显示"已撤回"
```

关键判断：

- **数据必经之处 = 最好的下手点。** 不管 UI 开没开，撤回要"生效"就**必须落库**。所以 **sqlcipher 的写入**是所有路径都绕不过的咽喉。
- 越靠近"业务逻辑"的层（rust 函数）越混淆、越易随版本变；越靠近"标准设施"的层（SQLite 的 `sqlite3_*` 导出、表名列名）越稳定。

**原则 2：在"数据必经"且"接口稳定"的层下手。咽喉点 > 业务函数。**

> 这一步直接决定了后面少走的弯路：我一开始也顺着 rust 函数
> `sub_51F4798`「[receive message] save_messages update_recall」往下挖 callee，
> 想 no-op 那个"清内容"的调用——**越挖越脆**（多层 rust、地址每版变）。
> 果断放弃，转去存储层。这是全程最重要的一次转向。

---

## 3. 在混淆海里定位：三把不需要"读懂全部"的刀

772k 个函数、符号全无。**不要试图读懂它**。用这三种手段，每一种都只需要"读懂一点点"：

### 刀 1：字符串锚定（静态，快速收敛）
混淆能改函数名，但**日志字符串、命令名、错误提示通常还在明文**。
- 搜 `recall` / `new_recall` / `save_messages` / `is_recall` 等，反查**引用这些字符串的函数**。
- 一下从 77 万收敛到几十个"和撤回直接相关"的函数。命中冒烟枪：`im.messenger.new_recall`（撤回命令名）、`[receive message] save_messages update_recall`（收消息+更新撤回的入口）。

**要点**：用"引用某字符串的函数"做集合，而不是"调用了某通用 helper（memcpy/分配器）的函数"——后者会被几千个函数共用，收敛不动。

### 刀 2：运行时触发-观察（动态，决定性）
静态看"哪个函数像是清内容"是**猜**；运行时看"撤回时实际发生了什么"是**证**。
- frida 挂上若干候选 + 咽喉点，**真的去触发一次撤回**，看谁亮、看实际写了什么。
- 把"启动同步噪音"和"这一次实时撤回"区分开（让对方发→撤，盯这一下）。

### 刀 3：before/after 同一对象 diff（一锤定音）
最强的证据是**同一条消息、撤回前后、同一行数据的差异**：

| 字段 | 收信(回执) | 撤回后 |
|---|---|---|
| `is_recalled` | **0** | **1** |
| `content`(protobuf blob) | `…防撤回测试BBB222…`(97 hex) | 空 stub `x'823204120038011200'`(23 hex) |

到这一步，机制 100% 确定：**撤回 = 对同一 id 再 `REPLACE INTO messages`，把 `is_recalled` 置 1、`content` 改成空 stub。** 不再需要任何猜测。

**原则 3：能动态验证就别静态硬啃；能拿到 before/after 实证就别停在"看起来像"。**

---

## 4. 抓存储层的两个真·坑（否则白忙）

把 hook 挂到 `libsqlcipher.so` 上时，有两个不踩会一直看不到东西：

1. **预编译语句被缓存复用**：App 启动时就 `prepare` 好、之后反复 `bind+step`。你 attach 时 `prepare` 早过了 → **hook `prepare` 啥也抓不到**。
   - 解法：hook **`sqlite3_step`**，在 step 时用 `sqlite3_sql()`(取模板) / `sqlite3_expanded_sql()`(取带值的完整 SQL) 现读。这俩对"任何时候 prepare 的语句"都有效。
2. **绑定值是参数化的**：`VALUES(?,?…)`，光看模板看不到内容。`expanded_sql` 会把绑定值展开成字面量，能直接看到 protobuf blob 和 `is_recalled` 的真实值。

**原则 4：先搞清目标设施"什么时候、以什么形式"暴露你要的数据，再决定 hook 哪个 API。**

---

## 5. 干预要"最小且不打架"

知道了"撤回 = 一次带 `is_recalled=1` 的 `REPLACE`"，干预方式有好几种，选**最小副作用**的：

- ❌ 直接"跳过这次 step"：frida 里要 `Interceptor.replace` 还得调原函数，易递归崩；且 ORM 可能因 rowcount 异常困惑。
- ❌ 还原 content + 置 `is_recalled=0`：需要缓存每条原文 blob，复杂。
- ✅ **改绑主键到哨兵**：step 前把 `id`、`chat_id` 这两个绑定参数改成 `(1,1)`。
  - 那条"被清空的撤回行"落到一个废弃主键 → 真正的原始行（收信时已入库、content 完整、`is_recalled=0`）**原封不动**。
  - 无需缓存、无需还原 content、不碰 step 控制流、只留一条无害垃圾行。

判别"这是不是撤回写入"：模板以 `REPLACE INTO \`messages\`` 开头 **且** 展开 SQL 里第 15 列 `is_recalled==1`。只认表名/列名/`sqlite3_*` 导出名——**飞书升级几乎不变**，比认 rust 函数地址稳得多。

**原则 5：在咽喉点做"改一个字段"级别的最小手术，让原始数据自然存活，别试图重建它。**

> ⚠️ 已知副作用（接受为 B 方案）：撤回时另插的"X 撤回了一条消息"系统提示行仍在（当标记）；
> 打开聊天时飞书"对账"会反复重发撤回被连拦（一阵后收敛，非死循环）；模块生效前已清的旧消息救不回。

---

## 6. 从"能用"到"免电脑常驻"：frida → LSPosed 自带 native hook

frida 版（PC + frida-server）先验证机制可行；要日常用就得做成插件。架构：

- LSPosed 模块（Java）在**主进程**里：把自带的 `libantirecall.so` 从模块 apk 抽到 App 私有目录 → `System.load` → 起线程轮询装 hook。
- `libantirecall.so`（NDK 编）：找到 `libsqlcipher.so`、解析它的导出、对 `sqlite3_step` 做 **inline hook**，逻辑同 frida 版。

### 这一段的调试方法论（比代码更值钱）

注入型 native 库会撞上一堆"正常编译跑不出来"的坑。处理它们的**统一方法**是：

> **崩了就读 tombstone：拿到出错 PC → `nm`/`objdump` 把 PC 映射到符号 → 形成一个精确假设 → 只改一个变量 → 重测。**

每个崩溃都教了一件具体的事：

| 崩在哪（tombstone PC→符号） | 真因 | 修法 |
|---|---|---|
| `__aarch64_ldadd8_acq_rel` | 64-bit 原子触发 outline-atomics，在注入库里没初始化 | `-mno-outline-atomics`，计数改普通 int |
| `.plt` 头 `br x17`，x17=未重定位的 PLT0 地址 | 默认 packed/RELR 重定位在 `System.load` 注入场景没被应用 | `-Wl,--pack-dyn-relocs=none -Wl,-z,now` |
| 自起 pthread 上第一个 PLT 调用 | raw pthread 上 PLT 惰性解析崩 | 改由 **Java 线程经 JNI 反复调 `tryInstall()`** |
| `dlopen(RTLD_NOLOAD)` 返回 NULL | 模块与 App 的 libsqlcipher 不在同一 linker namespace | 弃用 dlopen/dlsym，**读 `/proc/self/maps` + 手解析 ELF `.dynsym`**（namespace 无关） |
| `resolve` 全返回 0 | 假设了 dynsym 紧邻 dynstr（不成立） | 用 **GNU_HASH/DT_HASH 求符号数** |
| 选到 `bytehook-plt-trampolines` 段当 base | find_base 没要求文件 offset=0 | 只取 **offset=0 且以 ELF magic 开头**的段 |
| PowerShell 构建报 ParserError | `-Wl,-z,now` 里的逗号被当成数组 | 每个链接 flag **单引号**包成单 token |

**原则 6：注入 native 的崩溃几乎都来自"加载/重定位/命名空间/线程上下文"，不是你的业务逻辑。
让 tombstone 当向导，一次只动一个变量。**

---

## 7. 验证要"在最终形态、最严条件下"做

- 测的是**插件本身**：先**关掉 PC 上的 frida**，否则分不清是谁在生效。
- 测最难的场景：发到**未打开**的聊天 → 撤回 → 再打开看原文是否在。
- 看双证据：native 日志 `NEUTRALIZED`（命中）+ 手机上原文真的显示出来（效果）。

**原则 7：在去掉所有"脚手架"（PC/frida）后、用最严苛的用例验收，才算真成。**

---

## 8. 可复用的通用清单（下次换个 App / 换个目标照着走）

1. 写下**精确成功判据**，用约束砍方案。
2. 画数据流分层，找"数据必经 + 接口稳定"的**咽喉点**。
3. 混淆里定位：**字符串锚定**收敛 → **运行时触发观察** → **before/after 实证**。
4. 摸清咽喉设施**何时以何形式暴露数据**（如：语句缓存→hook step+expanded_sql）。
5. 设计**最小手术**式干预，让原始数据自然存活，认稳定标识（表/列/导出名）而非易变地址。
6. 先 frida 验证机制，再移植成常驻形态；注入 native 的坑用 **tombstone 驱动**逐个击破。
7. 撤掉脚手架、在最严用例下验收。

---

## 9. 关键文件索引（本项目）

- 机制验证脚本：`sql_trace3.js`（step+expanded_sql 抓写入）、`block_recall.js`（frida 版哨兵改绑）
- 成品工程：`xposed-antirecall\`
  - `native\jni\antirecall.cpp`（maps+ELF 解析 + inline hook）、`And64InlineHook.*`
  - `app\src\main\java\com\chekayo\feishuantirecall\AntiRecall.java`（主进程抽 .so + System.load + 轮询 tryInstall）
  - `build.ps1`（NDK 编 .so → 塞 apk → 签名）
- 记忆：`feishu-antirecall`（机制/坑/构建链）、`reverse-skill-toolchain`（工具链落点）

---

## 10. 用到的 MCP 与 Skill

### MCP 服务
- ✅ **`ida-multi-mcp`（IDA Pro / idalib 逆向）** —— 全程的静态分析后端，分析 `liblark.so`（77 万函数、符号全剥离）。实际用到：
  - `idalib_open` / `idalib_close`：打开/关闭已分析好的 `liblark.so.i64`（headless，无需 GUI）
  - `idalib_list` / `list_instances`：查看会话、避免多实例抢占许可
  - `list_funcs` / `lookup_funcs`：确认分析已加载、按地址/名定位函数
  - `analyze_function`：一次性拿伪代码 + 调用关系 + 字符串（核心手段）
  - `decompile`：完整反编译目标函数（如 `sub_51F4798`）
  - `callees`：追"清内容"调用链
- ⚪ 已连但本项目未用：`obsidian-mcp`（记忆是直接用文件工具改的）、`everything`（协议测试服）。

### 相关 Skill（本机 `.claude/skills/`）
- ✅ **`ida-reverse`**：IDA Pro 逆向（基于 ida-pro-mcp 2.0 / idalib headless）。静态分析的方法论与工具约定来源；`ida-multi-mcp` 即其后端。
- ✅ **`apk-reverse`**：APK 解包 / Java 反编译 / smali / 重打包 / **Frida 动态 Hook** / so 分析。本项目用其覆盖能力：Frida 抓 sqlcipher SQL、`adb` 装包、LSPosed 模块的 jadx/apktool/签名打包链（debug.keystore 来自此 skill）。
- ✅ **`reverse-engineering`**：通用逆向技术；"先动态定位、再静态确认、字符串锚定收敛"的整体思路与其一致。
- ⚪ 相关但未用：`radare2`（本项目用 IDA）、`mcp-js-reverse-playbook`（原生 .so，不涉及 JS）。

### 非 MCP 但关键的 CLI 工具链（多由上述 skill 在本机装好）
| 工具 | 用途 |
|---|---|
| **Frida**（16.7.19，自带 Java 桥） | 动态插桩：抓 sqlcipher SQL、验证机制、`drive.py` 常驻注入 |
| **adb** | 装包、强停飞书、读 `/proc/maps`、logcat、tombstone |
| **Android NDK r26d**（clang 17） | 交叉编译 ARM64 native inline-hook 库 |
| **idalib**（IDA `D:\IDA\ida93`） | headless 跑分析，被 `ida-multi-mcp` 调用 |
| **gh CLI** | 建仓、发 Release、提交 LSPosed 模块仓库 |
| 构建链 | javac(JDK22) → d8 → aapt2 → apksigner（LSPosed 模块打包） |

> 一句话：本项目 = **`ida-multi-mcp`（静态分析）** + **Frida（动态验证，属 `apk-reverse` 能力域）** + **NDK 自研 native hook** 三者配合；Skill 主要落在 **`ida-reverse` / `apk-reverse` / `reverse-engineering`**。

---

## 附:持久撤回提示改进

**起因**:原"哨兵改绑"法(撤回写入 id+chat_id 改绑 (1,1))只保住原文,但飞书那条"X 撤回了一条消息"
是内存临时渲染——位置飘忽、过一会消失,用户无持久的撤回痕迹。

**撤回写入结构**(实测 dump):`REPLACE INTO messages(...) VALUES(...)`,关键列 `is_recalled=1`、
`content=x'823204120038011200'`(小 protobuf `{f800:{f2:"",f7:1}}`)。撤回行的 id = 原消息 id(就地替换)。

**改进(方案1,已验证)**:检测到撤回写入(is_recalled=1)时,**只把 id 改成 `原id ^ 0x4000000000000000`**
(chat_id 保持真实、content 不动)。效果:
- 原消息行(原 id)不被覆盖 → **原文保留**;
- 撤回行落到一个新 id → 飞书在该会话渲染一条**持久的"撤回了一条消息"**,与原文并存、不再消失;
- 新 id 由原 id 异或高位得到 → 几乎不可能撞真实消息;重复撤回同一条 → 同一新 id → 幂等不重复。
代码:`native/jni/antirecall.cpp` 的 `my_step` 撤回分支(`recall_is_one` 命中)。

**未做到**:字面"(已阻止)"标注。撤回消息文案是飞书按 `is_recalled` 标志**写死**的,content 里塞文本不渲染
(实测 content.field2 填"(已阻止)"无效)。要显示自定义字样须把该行伪造成**普通文本消息**(is_recalled=0 +
文本 type_ + 文本 content protobuf),需文本消息 schema(type_/content 格式/是否加密),待后续补。

---

## 附:v7.70.13 失效与修复 —— UNIQUE(cid) 撞索引删原行

**症状**:升级到 **7.70.13** 后防撤回失效——撤回的消息原文丢失(只显示"对方撤回了一条消息")。

**排查(先证伪再定案)**:
1. **hook 没死**:实测 `sqlite3_step` hook 仍活着并触发(`RECALL-DIAG` + `撤回→持久提示` 满屏),`re-hooked` 计数 0。
   期间一度怀疑飞书反篡改 `PV-MON`(日志见 `PV-MON: hook libsqlcipher.so`,且 sqlite3_step 那页是 `rwxp`、
   字节曾被还原)——但当前会话未复现还原。**顺手补强**:`maintain_hook()` 加 sqlite3_step 入口16字节比对+周期重装
   (放在 `if(!g_649_installed)return` 之前,否则纯防撤回模式整个维护空转),Java 维护间隔 800ms→150ms。
2. **列序没变**:dump `REPLACE INTO messages` 列模板,`id(0)…content(9)…position(11) cid(12) is_recalled(14)`,
   `PARAM_ID/CONTENT/FIELD_IS_RECALLED` 硬编码偏移全部仍对。
3. **真根因**:dump 撤回 REPLACE 的 VALUES 发现——撤回行虽被改了 `id`,但**仍携带原始 `cid` 和 `position`**。
   飞书 7.70.x 给 **`messages.cid` 加了 UNIQUE 索引**;SQLite `REPLACE` 遇**任一**唯一约束冲突都会**先删冲突行**,
   于是撤回 REPLACE 凭相同 `cid` 把原消息行删掉——**只改 id 根本挡不住**。≤7.69 无此唯一索引,所以旧"方案1"当时有效。

> 教训:`REPLACE` 的危险在于它按**所有** UNIQUE/PK 约束删冲突行,不止主键。做"并存行"必须把**每一个**
> 可能撞的唯一列都改绑成独立值,否则原行被连带删除。

**修复(方案3,已验证:原文 + 撤回提示都在)**:撤回行 **`id` 与 `cid` 双改绑**成唯一值:
```c
long long nid = oid ^ 0x4000000000000000LL;
p_bind64(stmt, PARAM_ID, nid);                              // 新 id
char cidbuf[24]; snprintf(cidbuf,sizeof cidbuf,"ar%lld",nid);
p_bind_text(stmt, PARAM_CID/*13*/, cidbuf, -1, SQLITE_TRANSIENT); // 新 cid，避开 UNIQUE(cid)
```
新增解析 `sqlite3_bind_text`。撤回行不再撞 `cid` 唯一索引 → 原行(is_recalled=0,内容完整)与撤回行
(is_recalled=1,原 position)**并存** → 既见原文又见"对方撤回了一条消息"。
开关 `g_recall_drop`:`0`=持久提示(默认)、`1`=纯丢弃撤回写入(`return 101`/SQLITE_DONE 不执行,只留原文无提示)。

**若将来又失效(只剩提示、原文没了)**:说明 7.7x 又加了 `(chat_id,position)` 唯一索引,把 `position` 也一并改绑
(代价:撤回提示不在原位置)。诊断照旧:`g_recall_diag=1` dump VALUES 对比即可定位新增的唯一列。

**可复用诊断**:判 hook 死活 = `/proc/pid/maps` 取 libsqlcipher 基址 → `dd if=/proc/pid/mem`(skip 用**十进制**,
shell `$((0x..))` 大数会失败)读 sqlite3_step 入口16字节,与 `.so` 文件原始字节比对(相同=未 hook/被还原)。

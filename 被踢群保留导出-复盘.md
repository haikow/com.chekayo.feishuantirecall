# 被踢群聊天记录：保留 + 导出（复盘）

> 目标：被移出群聊后，本地聊天记录不被清空、且能看到。
> 结论：**能"留住 + 导出可读"，但不能在飞书 app 里强行点开看**（UI 门控在 native）。

---

## 一、被踢时飞书对本地库干了什么（sqlite3_step 诊断实测）
群主移出你 → 服务端下发 → native 处理 → 对本地加密库爆发式清理：
- `UPDATE chats SET role/is_admin/removed_visible_message_position ...`（标你非成员、设可见截断线；**群行本身不删**）；
- `DELETE FROM non_departmental_chatters ... chatter_id IN`（从成员表移除你）；
- `DELETE FROM chat_chatter_ref/chat_chatter_extra/chat_top_notice/... WHERE chat_id IN`（清群辅助数据）；
- **`DELETE FROM messages WHERE id IN (...)`**（删你的聊天记录）。

## 二、保留（SQL 层拦截，`g_keep_kicked`）
利用时序：清群时先爆发 `DELETE ... WHERE chat_id IN`（清群信号）→ 见此**开 3 秒窗口**：
- 丢弃这些清群 DELETE（`return 101` SQLITE_DONE，谎报完成、实不执行）；
- 窗口内**丢弃 `DELETE FROM messages`** → 聊天记录保住。
实测：消息不删、飞书不崩。

## 三、为什么不能在 app 里强行点开看（对比 Blued「别想玩消失」）
Blued 手术刀能让注销用户会话还能看，靠 **hook Java 层 `SessionRelationModel.getDelete()` 强改返回 0**。
飞书试了同思路——hook `com.ss.android.lark.chat.entity.chat.Chat` 的 `isMember/isDissolved/isDeleted`：**0 命中**。再查"你已不在该群聊"提示：
- Toast.makeText → 0 命中（走 lark 自定义 toast）；
- Resources.getText/getString(资源 id) → 0 命中（走 lark 自己的 i18n，不过 android 资源）。
**三个 Java 探针全落空 → 门控在 native**（聊天有效性由 native liblark 决策，i18n/toast 由 native 驱动）——就是防已读那堵 async Rust 墙。**Blued 门控在 Java 能做，飞书在 native 做不到。**

## 四、导出（绕开 native 门控，看被踢群历史的现实答案）
模块 hook 在 `sqlite3_step`、**跑在 sqlcipher 已解密的进程内**，可直接 SELECT 导出：
- 被踢触发 `do_export`：`SELECT id, from_id, create_time, content FROM messages WHERE chat_id = ?`；
- **content 是 protobuf blob** → `pb_extract_buf` 递归解析，只抠字符串字段（跳过 tag/长度/数字框架）+ UTF-8 校验 → 干净文本；
- **发送人昵称**：`chatters` 表在**另一个连接**（`g_chatters_db` 单独捕获），查 `SELECT name, alias, another_name`——**`alias`(备注名) 优先 > `another_name`(群昵称) > `name`(真名)**（实测 cheky 的 name 是真名"张三"、alias 才是"cheky"）；
- **群名**：踢群时 `chats.name` 已被清 → 平时从 `UPDATE chats SET name=?` 解析缓存（`g_names`）；
- **系统消息**：content 内含 `from_user <id>` / `to_chatters <id>`，`find_id_after` 抠 id → 查名 → 拼成中文「cheky 邀请了 张三」「cheky 移除了 张三」「cheky 把群名改为 X」；
- 结构化写 `kicked_<chatid>.txt`：`群名` 头 + 每行 `时间\t昵称\t文本`。

## 五、查看（面板，聊天样式 + 搜索）
设置面板 **📤 被踢群聊天记录**：
- **搜索框**：按群名 / 消息内容实时过滤；
- 群列表显示**群名**（读文件头，非 chatid）；
- 点开 = **聊天气泡**：昵称·时间 + 蓝气泡；系统消息小灰字居中；
- **复制 / 分享**（scoped storage 写不了 /sdcard，走剪贴板/ACTION_SEND）。

## 六、方法论沉淀
1. **删除型功能拦 SQL 写入**（同防撤回），不指望事后渲染。
2. **UI 可视性门控 = 看在 Java 还是 native**：Blued 在 Java（可 hook 改判定），飞书在 native（撞墙）——三探针（实体方法/Toast/Resources）快速证伪。
3. **门控破不了就绕开**：进程内直接读已解密库 + 自建查看器（Blued 也这么干后半段），达成"看历史"不依赖 app 的聊天窗口。
4. **跨库注意连接隔离**：messages 与 chatters 不在同一 sqlite 连接，需分别捕获 db handle（同线程同连接才安全）。
5. **昵称多列**：Feishu 显示名优先 alias(备注) > another_name(群昵称) > name(真名)。

*代码：`native/jni/antirecall.cpp`(g_keep_kicked/do_export/pb_extract_buf/get_chatter_name/build_system_line)、`FuckLarkSettings.showKickedExports/showKickedFile`。开关 `Config.keepkicked`(默认关)。*

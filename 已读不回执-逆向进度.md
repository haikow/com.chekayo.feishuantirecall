# 飞书「已读不回执」（Stealth Read）逆向进度与交接文档

> 本功能**未完成**，处于暂停挂起状态。本书记录截至目前的全部进度：已确认的机制、
> 关键代码偏移、踩过的坑、可复用脚本，以及下一步该往哪打。下次直接照本文续即可，不必重走弯路。
>
> 与同目录《防撤回-逆向方法论手册.md》是姊妹篇：防撤回拦的是「下行落库」（sqlite 层），
> 本功能要拦的是「上行网络上报」（已读回执发给服务器）。**咽喉点不同，方法论通用。**

---

## 0. 一句话现状

> 已读上报的命令、代码点、触发路径都定位到了；但真正的「发送」是**异步 + 加密**发出，
> 同步路径上的 hook 够不到，发送函数尚未拿下。卡点明确，不是无头绪。
>
> **【最新结论】**通用 frontier 命令发送 = `sub_59AFFEC`（已完整摸清：liblark .text、hook 安全、
> 带每命令 descriptor）。但**双账号实证：全拦它读时的 18 个 descriptor，对方仍显示已读 ⇒ 已读不走 sub_59AFFEC**。
> ⇒ **该方向作废**（详见 §8.9）。已排除：put_read / transmit(埋点) / auto_open(门控关) / sub_59AFFEC 全族。
> 已读真实发送通道仍未找到；新方向见 §8.9（另一套发送原语 / 状态 sync 而非命令 / QUIC / 从 read_position 数据流入手）。

---

## 0.5 本轮实验更新（Linux 机实测）

> 整套 RE 台子已迁到 Linux 机（非原 Windows）。设备 = 同一台一加13（PJA110, SDK33, USB serial
> `YOUR_DEVICE_SERIAL`），飞书 **7.69.6**；`~/lark_re/` 内有 7.69.6 的 `liblark-arm64-7.69.6.so` + 完整 IDA 库
> （`.id0/.id1/.nam/.til`）+ `libttboringssl-7.69.6.so`；NDK 26.3 + build.sh 一键出包均通。
> ⇒ 原 §8.5「7.69.6 没法 headless 静态分析」**已不成立**，可直接 r2/IDA 库静态分析。

**两个新的「双账号实证」结论（之前文档只有理论推断，这次是实验数据）：**

1. **实测1 — transmit-drop 无效。** `native` 里 cpp 早已写好的 `OFF_TRANSMIT 0x5aef148`
   （read send poll 内部"逐条派发读回执"的 transmit，单 caller 0x5aed840 read 专属、无 x8 sret、
   返回被忽略）重新启用、`g_drop_read=1` 丢弃返回。日志确认 **hook 命中**（`dropped read transmit #1`），
   但**对方端仍显示「已读」**。⇒ 这个同步 transmit **不是真实送出点**。
   （静态 r2 复核：0x5aef148 确为函数入口 `sub sp,#0xb0; str x30; stp x20,x19…; ldr self,[x0+0x38]`，与 cpp 注释一致。）

2. **实测2 — 统一打包点 packer 在主进程静默。** 改挂 cpp 的 `OFF_PACK 0x6111d8c`
   （号称"所有出站 cmd 必经的统一打包函数, w0=cmd id, x8=sret"）观测器 + 对 `cmd=0x28` 安全栈扫描。
   读消息时主进程内 **零条 `[PACK]` 命中**（连后台心跳等其它 cmd 都没有）。
   ⇒ 要么该地址非热点统一打包点（cpp 当初是静态猜测），要么读回执根本不经此同步打包点。
   （注：跨进程 `/proc/PID/mem` 读被 SELinux 拦，没能字节级确认 hook 是否装上；但同模块的 sqlite
   防撤回 hook 同进程正常工作，排除"模块没起来"。）

**进程拓扑实测：** liblark.so + 网络栈（`libsscronet`/`libttboringssl`/`libquick`）**全在主进程
`com.ss.android.lark`**（另有 `:wschannel` 进程持 socket、`:sandboxed_process0`）。⇒ 打包在主进程、
socket 写在 wschannel 进程；**send 跨进程 + 异步**，这正是同步 hook 够不到的根因。

**结论（升级 §4）：** 「真实已读上报 = 异步」从推断变成**实验坐实**——两条同步咽喉（transmit / packer）
都拦不住。**不要再沿"拦发送"方向打**，应转向"生成/特性闸"层（见 §8，下面已按新证据重排优先级）。

> 状态：实验后已把 native 模块**恢复为纯防撤回**（防已读 hook 全撤、日志 `PURE ANTI-RECALL mode`），
> 用户日用版干净。所有防已读探针/中和函数仍留在 `native/jni/antirecall.cpp` 里，靠 `install_probe()`
> 里加一行 `A64HookFunction` + 翻 `g_drop_read` 即可复跑；frida 路线在本机反调试拦截下 attach 易 hang
> （`frida-server` 需与客户端版本严格对齐，本机客户端 17.15.0，已推同版 server，但主进程 attach 仍超时），
> **native LSPosed 注入比 frida 稳**，建议续用 native 路线做实验。

---

## 1. 目标与精确判据

- ❌ 模糊：「不发送已读」
- ✅ 精确：**本地照常标记已读**（自己 UI 显示已读、红点正常清），**但服务器收不到已读回执**（对方看不到「已读」）。
- 隐含约束：要拦的是「上行网络上报」，**不能拦本地 DB 的已读写入**（否则自己 UI 也不显示已读，本末倒置）。这跟防撤回正相反。

行为需求（与产品方确认）：**默认屏蔽所有已读回执 + 预留「手动标记已读」开关**。

---

## 2. 环境与工具链

- 设备：一加 13（PJA110），Android 13 / SDK 33，**已 root**，无线调试 `<DEVICE_IP:PORT>`。
- 飞书版本：**7.69.6**（versionCode 7690650）。⚠️ 注意：`<本地 IDA 库路径>` 是 **7.52.4** 的分析库，偏移不同；本文所有偏移均为 **7.69.6 实测**。
- PC frida **16.7.19**；设备 `frida-server` 16.7.19（`/data/local/tmp/frida-server`，root 启动）。⚠️ 设备另有个 `frida-server-17`(17.11.0)，与客户端不匹配会导致 attach 报 closed——必须用 16.7.19 那个。
- adb 用本机 `C:\Users\zbj\Tools\platform-tools\adb.exe`（详见记忆 `adb-and-test-device`）。

---

## 3. 已确认发现（7.69.6 真机 frida 实测）

### 3.1 命令名锚点（字符串）
| 字符串 | liblark.so 偏移 | 说明 |
|---|---|---|
| `im.message.put_read` | **0x11b6991** | 已读上报命令名（唯一精确代码引用点 @0x5b36fc8，仅作日志） |
| `im.chat.auto_open_v2` | 0x11b68ed 附近 | 打开聊天上报（携带已读位置） |
| `put_read_threads_result`、`put_read_notifications_impl` | 0x1136dba / 0x10a0d55 | put_read 的子路径日志 |

### 3.2 打开聊天读消息时，**确实触发**的代码点（指令级 hook 实测命中）
| 代码偏移 | 加载的字符串 | 结论 |
|---|---|---|
| 0x59dc03c | `im.chat.auto_open_v2` | 触发，**但被门控关掉**（见 3.3） |
| 0x59dc0c4 | `im.chat.sdk_auto_open_translate` | 触发 |
| 0x59dc1b0 / 0x59dc394 | `read_message::logic #read put read da…`（tracing span） | 触发，但只是 span 日志，非发送 |
| 0x58771fc | `read_message::model #gen_put_req`（span） | 触发 |

**未触发**：0x5b36fc8（put_read 命令名日志，仅条件分支用）、0x5971de4、0x5a05f24（receive::consume）。

### 3.3 三个关键否定结论（省你重走）
1. **`auto_open_v2` 本来就没发**：门控函数 `0x5774c64(obj, "im.chat.auto_open_v2", 20)` 对该命令**本身就返回 0**，后面 `cbz w0` 已跳过构建/发送。我强制它返回 0 没改变任何事——这条不是已读载体。
2. **`gen_put_req` 那次调用是「生成后丢弃」**：`gen_put_req`(@0x58771d0) 触发 1 次，但其调用者（0x5aee604）把 sret 结果传给通用 helper `0x58da644`（几秒内被调 45 次，明显是通用 alloc/drop 类）后清理丢弃，**不是发送路径**。
3. **命令名不在加密前明文里**：挂 libttboringssl/libssl 的 `SSL_write`，读消息时大量触发（32 次/读），但缓冲区里**没有** `im.`/`put_read` 字样——put_read 走**数字 cmd**，非明文命令名。⇒ 无法按内容在 socket 层拦包。

### 3.4 网络栈
飞书加载：`libsscronet.so`（字节 Cronet）、`libttboringssl.so`（字节 TLS，导出 `SSL_write` @0x721f718bb0）、`libssl.so`（系统，SSL_write @0x72a8842a94）、`libquick.so`（QUIC）。IM 长连接大概率走 libttboringssl 或 QUIC。

---

## 4. 为什么难（诚实评估）

1. **异步上报**：请求在同步路径生成后**入队**，由网络线程发出。同步路径挂 hook 抓不到「发送」那一下；SSL_write 上的回溯是网络线程栈，**不含 read 代码**。
2. **加密 + 数字 cmd**：wire 上无明文命令名锚点，没法按内容识别 put_read 包。
3. **编排函数是巨型 inlined Rust**：约 0x59dbc40 起，几十个 `bl`、满屏 `b`/`br x10`，肉眼找不到提交点。
4. **Stalker 噪声**：从 auto_open 触发点跟踪 150ms，捕获 ~1300 个不同调用目标；即便过滤到「低频(≤5)+业务区(≥0x4000000)」仍有几百个，「提交」淹没其中无法唯一识别。

量级：跟当年防撤回同属「硬骨头 RE」，不是几轮能啃完。

---

## 5. 关键代码偏移速查表（7.69.6，base 随 ASLR 变，偏移恒定）

| 偏移 | 含义 |
|---|---|
| 0x11b6991 | 数据：字符串 `im.message.put_read` |
| 0x11b6986 / 0x11b699c | 同串邻近（`read_message::export` / `put_read` 子串） |
| 0x58771d0 | 函数 `gen_put_req`（生成 put_read 请求，sret 返回） |
| 0x5774c64 | 命令门控 `(obj, cmd_name, len) -> bool`（auto_open_v2 经此，返回0=关） |
| 0x57749b0 | 同族（put_read 日志路径的命令名调用，未触发） |
| 0x58da644 | 通用 helper（alloc/drop 类，高频，**非**发送） |
| 0x5aee600 | `bl gen_put_req` 调用点（其结果被丢弃） |
| 0x59dbc40 | 编排大函数起点（`sub sp,sp,#0x1d0`…，read_message::logic） |
| 0x59dc03c / 0x59dc0c4 / 0x59dc1b0 / 0x59dc394 | auto_open / sdk_auto_open / put_read span 加载点 |
| 0x5b36fc8 | put_read 命令名**唯一**代码引用（日志，条件分支未走） |
| liblark .text 段 | base+0x2fd5000（`r-xp`，见 /proc/PID/maps） |

---

## 6. 可复用脚本（均在 `stealth-read/scripts/`）

### 6.1 最值钱：arm64 `adrp+add` 静态 xref 扫描器 —— `read_xref.js`
在剥离符号的 rust .so 里「找引用某字符串的代码」。**核心坑**：JS 位运算 `&` 返回**有符号 32 位**，必须 `((x & mask) >>> 0)` 再与无符号字面量比较，否则所有 adrp 都解不出（真实坑：合成 `0x90000002` 一开始返回 null）。解码 adrp 取 immhi:immlo<<12，加 `(pc & ~0xfff)`，配对下一条 `add imm` 验证寄存器与页内偏移。

### 6.2 frida 常驻驱动 —— `drive_read.py`
用法：`python drive_read.py <lark_pid> <script.js>`。attach 后 `script.on('message')` 把 JS `send()` 的命中写进 `read_obs.log` 并带时间戳，`while True: sleep(1)` 保活。**为什么需要它**：后台非 TTY 下 `frida -q` 装完脚本即退出，hook 不保活。

### 6.3 脚本一览
| 脚本 | 作用 |
|---|---|
| read_recon.js | 摸 liblark 模块/网络库/SSL_write 导出 |
| read_scan.js | 按可读段扫 `put_read` 字符串定位+上下文 |
| read_xref.js | **adrp+add 静态 xref 扫描器**（找命令名代码引用） |
| read_obs.js | 挂 SSL_write 抓含 `im.`/`put_read` 的明文包+回溯（结论：抓不到，数字cmd） |
| read_hook.js | 挂 gen_put_req + 候选分发点，看谁在「读」时亮 |
| read_probe.js | **撒网**：9 个已读相关代码点指令级 hook，定位真实触发路径 |
| read_block.js | NOP 掉 0x5aee608（失败：那是丢弃路径） |
| read_block2.js | hook 0x5774c64 强制返回0（失败：本就返回0，门控已关） |
| read_stalker.js / read_stalker2.js | Stalker 跟踪读线程调用（噪声大，未收敛） |
| read_disasm[2-5].js | 反汇编 gen_put_req / 调用者 / auto_open 周边找发送 |

---

## 7. 坑 / 经验（下次别再踩）

- **frida `Memory.scanSync` 扫整个 module 会撞不可读页**（段间空洞报 access violation）→ 改用 `module.enumerateRanges('r--')` 分段扫。
- **`enumerateRanges('r-x')` 可能含 `.eh_frame` 数据页**（非代码，开头是 `0x24 0x24…`，反汇编全 `udf`）→ 真正 .text 看 `/proc/PID/maps` 的 `r-xp` 段（**需 `su -c` 读**，普通 shell 读不了别人的 maps 得空）。
- **arm64 解码 `&` 后必须 `>>> 0`**（见 6.1）。
- **后台非 TTY 下 `frida -q` 装完即退** → 用 python 常驻驱动。
- **`Memory.patchCode` 的 patch 在 frida 断开后不自动还原** → 需重启进程或显式写回原指令。
- **Feishu 进程会随机重启**（pid/base 变），但 liblark.so 内**偏移恒定**；脚本里统一用 `base.add(off)`。
- 设备 frida-server 必须用 16.7.19（与客户端匹配），别用 frida-server-17。

---

## 8. 下一步（按性价比排序，下次照此推进）

> **实验后重排（见 §0.5）：** 已用双账号实证「拦同步发送」死路（transmit/packer 均无效，
> 真实 send 异步+跨进程）。**新优先级：先打"不让它生成"，别再打"不让它发"。**
>
> **✗ A（特性闸/flag）—— 已查，是死路，无可翻开关。** 静态结论：
>   1. cpp 旧标 `fg im.message.put_read 闸 @0x5b36a88` **标错了**：r2 看其加载的串是 `rust_messenger_config`，
>      函数体 ~10KB、构造一个 config 对象、`ret` 返回 `x0=对象指针`(非 bool)；唯一 caller 0x59750f8 是
>      config 构造链（连串 config key），**不是 put_read 门控**。
>   2. 二进制里**没有**"关闭已读回执"的 feature flag——只有群"已读状态显示"列 `show_read_state_type/limit`
>      和**邮件** `need_read_receipt`，都管不到 IM 发送方上报。
>   3. put_read 生成 = `gen_put_packets 0x5aec998`（内含 `movz w0,0x28; bl PACK 0x6111d8c`），但它和 transmit
>      的 caller（0x5bd9290 / 0x5aed840）**静态 bl 调用方均为 0** ⇒ 经 async future vtable **间接调用**。
>      没有同步的 `if(flag){生成}` 决策点可翻。
>   ⇒ Path A 放弃。下面 B/C 才是活路。
>
> **✗ B（生成层伪造 put_read 空结果）—— 已查，作废（put_read 不是开聊天的已读载体）。**
>   做法本身没问题（输出是 `Vec<Packet>{ptr,cap,len}`，len@0x10，元素 0x60；安全造空=先调 orig 再把 len@0x10 清零，
>   保 ptr/cap 合法让 Drop 正常 dealloc）。**但前置实测推翻了前提**：
>   - 在主进程装 `gen_put_packets 0x5aec998`(GPP) + `0x6111d8c`(PACK) 两个 naked 跳板，install 后打印
>     `g_orig_gpp/g_orig_pack` **均非空** ⇒ hook 确实装上了（跳板 br 目标在 A64InlineHook 重定位区 0x77318480xx）。
>   - 开 1:1 聊天读消息：**GPP 零命中**；后台空跑 25s：**PACK 也零命中**。
>   ⇒ `0x6111d8c` 根本不是"统一打包函数"（cpp 旧注静态猜错，它只被 put_read 生成调用）；**整条 cmd 0x28/put_read
>     路径与"开聊天→对方看到已读"无关**。doc 全程打 put_read 是打错了命令。
>   ⇒ **Path B 放弃。**（注：transmit `0x5aef148` 是读路径里唯一会命中的点，但 drop 它无效——见 §0.5 实测1。）
>
> **➡ 重定向已有抓手（静态补充）：**
>   - **模块锚点**：`lark-api-message/src/logic/message/dependency_v2/read_status.rs` —— IM 已读状态处理就在这个
>     rust 模块；附近有 frontier 日志串 `chat_id= message_position= read_count= unread_count= is_recall= ...`。
>     **下次优先反编译 read_status.rs 里的"上报"函数**（找它发哪个 cmd、在哪入队）。
>   - **命令名候选**（注意：`im.chat.*`/`im.message.*` 这些点分串**多是 feature-gate key 不是 frontier 命令名**，
>     真命令是数字 cmd、经 bincode/postcard 序列化进加密帧）：`im.chat.auto_open_v2`(0xfb11bd)、
>     `im.message.send_message_with_read`、`im.chat.auto_open_v3`。
>   - **SSL_write 法已就绪**：GOT 槽 `0x60de610` 经 r2 reloc 实证 = `SSL_write` 导入（plain-C 可 hook，cpp my_sslwrite 现成）。
>     SSL_write 收到的是**加密前明文 frontier 帧**；读窗口内 dump 帧 + 与空闲窗口对比 = 认出已读命令。
>     **坑**：帧是 bincode/postcard 二进制、cmd 非固定字节偏移，需按结构对比、噪声大（doc §4.4 的老墙）。
> **➡ 已定位"开聊天处理器"（静态）：`read_message::logic` 函数 @ `0x59dbc40`** —— 读消息就走它。
>   关键内部调用：`0x59dc04c bl 0x5774c64`(命令门控/FG 闸，doc §3.3#1 说对 auto_open 返 0)、
>   `0x59dc458 bl 0x5aed350`(已读 send poll → 内部到 transmit 0x5aef148)。**真实已读上报的 send 就埋在这棵
>   ~50 个 bl 的 async 子树里**。`im.chat.auto_open_v2`(0xfb11bd) 经 xref 实证 **0 个 adrp+add 代码引用**
>   ⇒ 它是 FG-table key 不是命令串，别再当命令名追。
>   **新工具**：`stealth-read/scripts/xref_data.py`（向量化 adrp+add 数据 xref 扫描器，补 IDA 全局 xref；
>   `blscan.py` 出 bl 调用方）。两者够离线定位函数，但**反编译仍需 IDA**。
>   **IDA 卡点**：idalib headless 打开本 113MB 二进制要跑 1-2 小时全分析，远超工具单次 timeout（900/1500s 都被杀），
>   .id0 已涨到 ~860MB 仍没完 ⇒ **idalib 这条不实用；要反编译请在桌面 IDA GUI 打开 `~/lark_re/liblark-arm64-7.69.6.so`
>   的现成 DB**（已分析过一轮），反编译 `read_message::logic 0x59dbc40` + poll `0x5aed350` + transmit `0x5aef148`
>   子树，揪出真正 send 的那一跳和它的 cmd。
> **‼ 重大纠正（IDA GUI 反编译实证）：transmit `0x5aef148` 是埋点遥测,不是已读上报!**
>   桌面 IDA GUI 已连上(ida-multi-mcp 实例),反编译这条链:
>   - `transmit 0x5aef148`：循环 `sub_5A36248`(迭代器 next),每条调 `sub_5AEEFA0`。
>   - `sub_5AEEFA0` → `sub_5C2D778(&"read_request_event"@0x11cec18, 18, payload)` → `sub_5C2D778`→`sub_60B8C5C`。
>   - `sub_60B8C5C` 满是 **`lib_apm::tea` / `TeaData` / `SteamReportEvent` / "function to send tea data"**
>     —— 这是**字节跳动 Tea 埋点/遥测**系统,不是 frontier 上行!
>   ⇒ **transmit 只是逐条把 "read_request_event" 写进埋点**,cpp 旧注"逐条派发读回执给frontier上行"**标错了**。
>   ⇒ 这**彻底解释**了 §0.5 实测1"drop transmit 无效"：当时只关掉了遥测,根本没碰真实回执上报。
>   ⇒ transmit / 0x5aef000-0x5aef1cc 整簇(sub_5AEEFCC/5AEF00C/5AEF1CC 等)都是读相关**埋点**,**全部排除**。
>
> **➡ 真实已读上报仍未找到,且确认是 async**：chat-open handler 在 `0x59dc458` 调 poll `0x5aed350`,
>   而 `0x5aed350` 反编译 = `BR X10`(future vtable 间接派发) —— 静态跟不下去。put_read/transmit 两条具体路
>   都被证伪。**下一步只能动态**：在设备上 hook **通用 frontier 发送函数**(出站所有 cmd 必经,非 0x6111d8c/非埋点),
>   读时 log 所有 cmd id → 认出真正的已读命令。找该发送函数:从 put_read 请求提交链(gen_put_packets caller
>   0x5bd9290 所在函数 → 其 submit 调用)定位,或 IDA 里全量分析跑完后看 frontier::send 类符号。
> **➡ IDA 现状**：GUI 已连(实例 binary=liblark-arm64-7.69.6.so, imagebase=0, 偏移直接对应);
>   小函数反编译干净,但大 async 函数 IDA 自动定界失败(给不出函数体),需手动 define 或等全量分析+人工重建。
>
> **✗ 静态找"通用 frontier 发送函数"已穷尽**：
>   - 顺 put_read 提交链扒：builder = `sub_5BD8EE0`(0x5bd8ee0-0x5bd95b8, async, 反编译 15KB 带 positive-sp,
>     乱)。它的 callee 按调用方数排：memcpy/`sub_302D58C`(3688,alloc 类)/`sub_6481734`→`sub_6485710(1)`(1194,
>     运行时原语非 send)/`0x6111d8c`(29,family packer)... **没有一个是 frontier send**。
>     ⇒ builder 只建请求,**send 在 async runtime 层(channel enqueue→executor→网络 task),间接派发,静态链到不了**。
>   - `0x6111d8c` 有 **29 个 caller**(=29 个命令的 family packer),但读时 + 后台 25s 全零命中 ⇒ **读命令不在这 29 个里**,
>     用的是另一个 family packer(未找到,async)。
>   ⇒ **静态到此为止**。剩下两条都偏经验/动态:
>     **(I) SSL_write 读窗口相关法 —— 实测被反篡改封死。**
>       先纠正:SSL_write 的 .got.plt 槽真值是 **`0x6c4d528`**(r2 `ir`: SET_64 SSL_write;旧 `0x60de610` 是 .text 误值,
>       照它读指针 hook 直接崩)。改用 **GOT 重定向**(mprotect 改 liblark `0x6c4d528` 槽→my_sslwrite,不碰
>       libttboringssl 代码):install 成功(slot=base+0x6c4d528, orig=真 SSL_write@…bb0, 日志都打了),
>       **但 ~0.5s 后进程 signal 9 (SIGKILL)** —— 字节**反篡改**检测到 .got.plt 被改、直接杀进程。
>       (对照:libsqlcipher 的 sqlite3_step inline hook 全程稳;只 liblark 的 GOT 改动触发杀进程。)
>       ⇒ 原生改 SSL_write(GOT 或 inline)都被反篡改杀;frida 又因反调试 attach 挂死。**SSLW dump 这条堵死。**
>     **(II) 找 channel enqueue / 网络 task**：定位 frontier mpsc 的 send 端(出站所有 cmd 必经的入队点),
>       但无符号、async,静态难;更适合在 SSL_write 认出 cmd 后再反查。
>
> **★ 关键战略结论(,反篡改边界实测)：**
>   - **反篡改保护 `liblark.so`,不保护 `libsqlcipher.so`**:sqlite3_step inline hook 全程稳(防撤回一直工作);
>     而任何对 liblark 的代码/GOT 改动 → **SIGKILL**。⇒ **凡是在 liblark 里拦已读上报的方案(transmit/packer/
>     gen_put_packets/SSL_write/channel enqueue)本质上都会被反篡改杀**,不只是难找。
>   - frida(Interceptor 不改原码、走自己 trampoline)理论能绕反篡改,但本机 **attach 反调试挂死**;
>     要走 liblark 动态法须**先解决 frida attach**(换版本/魔改 frida-server/spawn-gating 等)。
>   - **唯一不被反篡改挡的层 = sqlite(libsqlcipher)= Path C**。已读上报若**读 DB 的 read_position 去发**,
>     可在 sqlite3_step 层动手(基建现成)。**要害**:必须只影响"上报读取"不动"本地已读显示";且先确认上报到底
>     读不读 DB(很可能读内存态,则 Path C 也不通)。这是反篡改封死 liblark 后唯一值得验证的活口。
>
> **底线(已修正)**:7.69.6"防对方已读"= 反调试(frida 挂)+ async/加密 + 反篡改**只盯 liblark .got.plt**。
>
> **‼ 修正上条过宽结论 + IDA 啃出 chat-open 读 future：**
>   - **反篡改边界精确化**:改 liblark **`.got.plt`** → SIGKILL;但 liblark **`.text` inline hook 安全**
>     (transmit/PACK/GPP 三个 .text hook 全程跑过没被杀)。⇒ **liblark .text 函数可安全 hook**,
>     之前没生效纯粹是"没找对函数"。**这把动态法重新打开了。**
>   - **SSL_write hook 可用且不被杀**(hook libttboringssl .text,非 liblark),但那层是 HTTP/2+应用层加密
>     (`82 fa` 固定 128B 加密帧),读命令在 SSL_write 看不到明文 ⇒ wire 层认不出读回执。
>   - **IDA 反编译定位到 chat-open 读 future = `sub_5AED840`**(读消息时跑;其 poll 入口 `0x5aed350`=BR X10
>     间接派发的就是它)。它 3 个独占 callee:
>       · `0x5aef148` = transmit(**埋点**,已排除)
>       · **`sub_5AECBA8` = put_req 构造**(tracing span `"lark_message::read_message::logic"`@0x11b6a71;
>         含 **"gen empty put req"** —— 开聊天没具体消息→生成**空 put_req 但仍带 chat 读位置**,
>         这解释了为何 packer 0x5aec998 没触发却仍上报已读)
>       · **`sub_5AEDB20` = 发送派发**(`→ sub_5AEEE50 → sub_5AEEE84/sub_5BFEA50/sub_5BD0324` async 发送链)
>   - **实验结果( 双账号实证):neuter `sub_5AEDB20` 命中(dropped #1)但对方仍已读 ⇒ 不是载体。**
>     连同 transmit(埋点)、gen_put_packets(开聊天不触发),**read_message/put_read 整条子系统已排除**——
>     它在 chat-open 确实"gen empty put req"=空操作,真实已读上报不经它。(.text hook 全程稳、没 SIGKILL,
>     再次印证 liblark .text 可安全 hook;问题始终是"找对函数"。)
>
> **➡➡ 收敛到唯一未验证子系统:chat auto_open / read_position( 当前战线)。**
>     chat-open handler(`read_message::logic` 含 `0x59dc458`)里 `0x59dc04c bl 0x5774c64` 那条命令门控走的就是
>     `im.chat.auto_open_v2`(doc §3.1"携带已读位置")。下一步:IDA 从 `0x5774c64`(门控,入参=命令名串+len)的
>     **caller 上下文**反查 auto_open 的"构造+发送"函数(类比 read future 的 sub_5AECBA8/sub_5AEDB20),
>     或找 `read_position` 同步上行函数;定位后 .text neuter + 双账号实测。**这是目前唯一没撞死的活线。**
>
> **chat-open handler 门控段反编译(,r2)**:`0x59dc04c bl 0x5774c64` 之后是 `cbz w0,0x59dc0c0`——
>   handler 里有**两个带"已读位置"的门控块**,都把 `[x19,0x110]`(q0,16B)+`[x19,0x120]`(8B,=read_position 数据)
>   塞进结构再发:
>     · 块1 `0x59dc054-08c`:命令串 `im.chat.auto_open_v2`@0x11b68ed(len20),gate=0x5774c64 → **cbz 跳过(门控关,doc 已述)**,发送函数 `0x5b6ef5c`。
>     · 块2 `0x59dc0d4-0f0`:命令串 `im.chat.sdk_auto_open_translate`@0x11b6901,gate=0x5c28534 → 发送函数 `0x5b6fcb8`(翻译特性,通常也关)。
>   两块都门控(且常关)⇒ **正常 1:1 开聊天的已读位置上报不在这两块**,而在门控之后的**无条件段**
>   (`0x59dc0f4` memcpy 0x80 起 → 一串无条件 bl → poll `0x5aed350`@0x59dc458)。
>   `0x5b6ef5c`/`0x5b6fcb8` 经 blscan 各**只 1 个 caller**(就是那两个门控块),没被复用 ⇒ 排除,载体不在这俩。
>   **下一步**:反编译/trace handler **无条件段** `0x59dc0f4..0x59dc458` 的一串 bl,找把 read_position 真正发出去的那一跳;
>   或直接 trace poll future 之外的无条件 bl。**这是当前最后的具体战线(async 大函数,需逐个试,同前 6 个候选的节奏)。**
>
> **★ 找到通用 frontier 发送 = `sub_59AFFEC`(0x59affec)+ 命令 descriptor 法：**
>   - 反编译链:chat-open 的 auto_open send `sub_5B6EF5C` → `sub_59AFFEC(channel, request, &desc, ...)`,
>     **arg3(x2)= 每命令唯一的 descriptor**(auto_open 的是 off_6A29898)。`sub_59AFFEC` 静态 8 个 bl caller,
>     但**运行时经间接调用几十次**=通用发送(所有出站命令必经)。它是 liblark .text → **可安全 hook(反篡改不杀)**。
>   - **descriptor trace 法(已跑通)**:naked 跳板保 x8、记录 arg3(desc)。**稳定空闲 0 命中**(非心跳),
>     开聊天读消息时一次冒出 **16 个不同 desc**(0x6a29xxx 命令表;auto_open=0x6a29898)。
>     单次读"恰好 1 次"的 desc(=once-per-open 候选):`0x6a29078`(caller 0x5aed308 read 簇)、
>     `0x6a29b98 / 0x6a29ab8 / 0x6a29a98 / 0x6a29998 / 0x6a295b8`。高频的(11/8/6 次)是按消息拉取等。
>   - **实测排除**:单拦 `0x6a29078` → 命中但对方仍已读(非载体)。
>   - **‼ 教训**:批量拦 5 个 once-per-open 候选 → **手机收不到消息**(其中一个是消息接收/长轮询命令)!
>     ⇒ `sub_59AFFEC` 误伤面极大,**绝不能整族盲拦**;必须**逐个单拦 + 同时验证"收发消息仍正常"**。已恢复干净模块。
>   - **下一步(安全版)**:对剩余候选 `0x6a29b98/0x6a29ab8/0x6a29a98/0x6a29998/0x6a295b8` **逐个单拦**,每次双账号测
>     (a) 对方是否已读 (b) 自己收发是否正常。命中"对方不已读且收发正常"的那个 = 干净阻断点。
>     （也可先 IDA 解析这些 desc 的命令身份:descriptor 是 trait vtable,需追其方法/关联名表,暂未解出名。）
>   - **descriptor 结构( 实测)**:`0x6a29xxx` 处是 **Rust trait object vtable**——交替排布
>     `{fn_ptr, size, align}`(auto_open 0x6a29898: slot0=sub_58DD764, slot3=sub_5B6F0E0, …, slot15=sub_5BC4600;
>     候选 0x6a29998: slot3=sub_5AF47DC, slot15=sub_5B94A8C)。**没有内联命令名串**,命令身份要靠调某个 vtable 方法取。
>     send 链 `sub_59AFFEC → sub_57B62E8 → sub_64A0364(…, &off_69FF4F0)`(逐层 thunk,反编译退化,未解出取 cmd-id 的槽)。
>     ⇒ 想纯静态命名 desc,得继续追 sub_64A0364/off_69FF4F0 怎么用 vtable 取命令号。**未完成。**
>   - **安全续法建议**:逐个单拦在**主账号**上有把握会再弄坏接收(已踩),建议**用测试账号/备机**做单拦扫;
>     或先把 desc→命令名静态解出(上一条)再精确单拦,避免在主号上试错。
>   - **静态命名已确认走不通**:enqueue 链 `sub_59AFFEC→sub_57B62E8→sub_64A0364→sub_649FE5C`;
>     `a3` 是 **uint8 优先级**(`a3<=0xE→队列1 否则2`),**不是命令号**;命令身份只在 descriptor(vtable)/序列化请求里,
>     **没有可记的整型 cmd-id**。⇒ descriptor **只能按地址区分**,无法静态命名。**只能靠设备单拦实测认。**
>
> ## 8.95 ★★ frida attach 突破成功—— 可对 lark 主进程注入 frida 了！

**之前 frida 进不去的真因(分两层)：**
1. **直接 attach 被反调试挡**：`dev.attach(lark_pid)` → "timed out waiting for stop"（TracerPid=0,非自我 ptrace;是 309 线程+反调试主动抗 stop）。良性进程(systemui)attach 秒成 → 确认是 lark 专属反调试。
2. **spawn 注入也被环境扫描挡**：`dev.spawn` 能挂起注入(绕过 ptrace 抗 stop),但 resume 后 lark **疯狂 shell 扫描**(getprop/`ps|grep frida`/mount/ip 几十次,设备级 spawn-gating 看得一清二楚)→ 检测到 frida-server(名字含 frida / 默认端口 27042)→ **主进程不加载 liblark/退出**,真正主进程换地方干净重启。

**突破配方(实证可用,稳定 35s+ SSL_write 持续命中、不被杀)：**
1. **改名 frida-server 躲 `ps|grep frida`**：`cp frida-server-1715 /data/local/tmp/dmtkd`(名字不含 frida)。
2. **自定义端口躲 27042 扫描**：`su -c "/data/local/tmp/dmtkd -l 0.0.0.0:8899"`。
3. **客户端走 remote**：`adb forward tcp:8899 tcp:8899` + `frida.get_device_manager().add_remote_device("127.0.0.1:8899")`。
4. **spawn-gating 注入主进程**：`dev.spawn(["com.ss.android.lark"])` 挂起→attach→load 脚本→resume;若该进程没加载 liblark(落进 sandboxed/husk)就 kill 重试,直到 hook 上 liblark。脚本里轮询 `Process.findModuleByName('liblark.so')` 到了再 hook。
   - ⚠️ 客户端版本必须配 server:本机 frida 17.15.0 ↔ 已推 frida-server 17.15.0(改名 dmtkd)。
5. 结果:**HOOKED main pid=…,liblark base 拿到,SSL_write(libttboringssl)hook 命中、帧持续流入,lark 不被反调试杀。**

**可复用件(都在 `stealth-read/scripts/`)：** `spawn_drive.py`(已配 remote 8899 + spawn 重试落主进程)、`hunt.js`(等 liblark→hook SSL_write,可改成任意 frida hunt)。
**意义:frida Interceptor/Stalker 现在能在 lark 主进程跑了**(不改原码、反篡改不杀)——这是穿透 async 间接派发、动态跟 read_position 数据流找已读发送的**正确工具**。下一阶段:用此 rig 做已读数据流跟踪(Stalker / hook+frida backtrace)。

**已读数据流 hunt 已就绪(hunt.js v3, 待稳定窗口跑)**:hook sqlite `UPDATE chats SET read_position`(A 读的可靠触发)
→ 命中后 `Stalker.follow` 当前线程 60ms,抓它在 liblark 里调用的函数集 = 读位置上报的构造/入队链。
跑法:`adb forward tcp:8899; python3 spawn_drive.py hunt.js`,A 读一次,看 `STALK targets`,在里面找发送/入队函数 → frida 直接 neuter 测(免重编)。

**⚠ 第二晚操作障碍( 后段)**:frida-server(改名 dmtkd)**起来几秒就被杀/退出**(疑反 frida 升级为主动杀 server),
spawn 也时常落 husk(in-process agent 扫描偶尔赢)。**突破本身已证**(曾稳定 hook 主进程 5 分钟),但当晚后段难维持稳定窗口。
**缓解**:① dmtkd 用 `setsid` 完全脱离 adb shell;② 设备侧 `while true; do dmtkd -l :8899; done` 看门狗自动重启;
③ 最干净=**重启手机**拿干净状态再起 dmtkd;④ 再考虑魔改 frida(strongR/hluda)隐藏 in-process agent 特征(gum 串/线程名)躲 in-process 扫描。
拿到稳定窗口后,hunt.js v3 一把就能定位已读发送(这是当前最有希望的收尾)。

**第二晚最终诊断( 深夜)——三层反 frida,已破前两层,第三层是 in-process agent 扫描:**
- ✅ 第1层 ptrace-attach 抗 stop → **spawn-gating 破**(挂起注入)。
- ✅ 第2层 外部扫描(`ps|grep frida`+端口27042) → **改名 dmtkd + 端口 8899 破**。
- ❌ 第3层 **in-process agent 扫描**:frida agent(gum)注入进程后,lark 扫自己 maps/线程名(gum-js-loop/gmain 等)发现 frida →
  **主进程放弃加载 liblark(husk)**。表现:`spawn_drive.py` 重试 13/13 落 husk(早期偶尔 attempt-1 命中=状态干净的窗口,
  夜里反 frida"热"了后几乎必 husk)。另 dmtkd 也被**主动杀**(看门狗 `while true; do dmtkd; done` 可续命)。
- **hunt.js v3 已证可用**:命中主进程那次 `sqlite_resolved/sqlite_hooked` 成功安装(Stalker-on-read 就绪),只差一个稳定主进程窗口。
- **破第3层的两条路(下次)**:
  1. **魔改 frida**(strongR-frida / hluda,编译/下载 17.15.0 arm64):重命名 gum 库、改线程名、抹 frida 串 → 躲 in-process 扫描。**首选**。
  2. **从我们自己的 LSPosed native 模块里 neuter lark 的 in-process frida 检测**(我们已在 lark 进程内、早于其检测跑):
     找到 lark 扫描 maps/线程名的那段,hook 掉让它"看不见"frida → agent 存活 → 主进程正常加载 → hunt 可跑。
  3. 临时土法:反复重试(状态干净时 attempt-1 能中)、或重启手机拿干净窗口再快速跑。
- **可复用件就绪**:`spawn_drive.py`(remote 8899+spawn重试)、`hunt.js`(v3=sqlite read_position 触发+Stalker)、dmtkd(改名 server)、看门狗一行。

---

## 8.9 ‼ 决定性结果：已读不走 sub_59AFFEC（双账号实证，整条线作废）

**做法**：单拦扫 + 全拦验证。结论硬：
- **5 个 once-per-open 候选(0x6a29078/b98/ab8/a98/998/5b8)逐个单拦**：全部"对方仍已读"。其中 0x6a29ab8/0x6a29a98 单拦会**弄坏会话列表刷新**(=feed/列表更新命令，非接收；消息点开仍在)。
- **纯接收(B 发、A 不点开)实测**：竟触发**全部 18 个 descriptor**(90 次)⇒ 这些 descriptor 收消息/读消息**都用、是复用的**，descriptor 不按"用途"分，无法靠它区分已读。
- **全拦 18 个 descriptor**：A 仍能点开看消息、**B 仍显示已读** ⇒ **已读根本不经 sub_59AFFEC(通用 frontier 命令发送)**。
- ⇒ **整个 sub_59AFFEC / descriptor 方向作废**(虽然把这条通用发送通道完整摸清了，但不是已读载体)。

**已读的真实发送通道仍未找到**，且已排除：put_read、transmit(埋点)、auto_open(门控关)、**sub_59AFFEC 全族**。
**深一层 `sub_57B62E8`(10 caller, 9 个绕过 59AFFEC)也试了**:hook 它 trace caller,**收/读均 0 命中**——
但 sub_59AFFEC(收消息时 90 次)按反编译是 tail-call 它的,本应同步触发 ⇒ **要么该 hook 静默失效(A64 在此函数失败),
要么 sub_59AFFEC→sub_57B62E8 是反编译退化的误读**。无论哪种,这条没给信号,**深层入队方向暂搁置**。

候选新方向(下次)：
  1. **另一套发送原语**(非 sub_59AFFEC)：feishu 可能有多个 frontier client / 多条 RPC 通道；已读可能走"chat 服务"自己的发送或一条独立 RPC。
  2. **状态同步而非命令**：A 读后更新本地 read_position，可能由一条**状态 sync/long-poll**(diff 推送)上行，而非离散命令发送 → 这类不经命令发送函数。
  3. **QUIC**：若已读走 libquick(QUIC) 而非 TLS，SSL_write/此 frontier 路都看不到。
  4. 重新从"A 读 → 哪个函数读 read_position 并发起上行"这一**数据流**入手(IDA 追 read_position 字段的 use→send)，而不是从发送函数反推。

---

## 9. 验证计划（历史：descriptor 单拦法，已证 sub_59AFFEC 非载体，仅留作记录）
>
> **目标**:在 16 个开聊天 descriptor 里,逐个单拦,找出"拦了→对方不显示已读、且自己收发正常"的那个=已读载体。
>
> **基建现成**:`native/jni/antirecall.cpp` 里 `sub_59AFFEC` 的 hook(`my_frontsend` naked 跳板 + `g_drop_descs[]`
> 按 arg3 过滤丢弃)已写好并验证可用。当前 install_probe 里已撤(纯防撤回)。**复跑只需**:在 install_probe 加回
> 那段(`g_drop_descs[g_drop_n++]=base+0x????`)、**一次只填一个 desc**、build.sh、装、双账号测。
>
> **务必用测试账号/备机**(主账号已被批量拦弄坏过接收)。每次测两件事:**(a) B 端是否还显示已读 (b) 手机收发是否正常**。
>
> **16 个 descriptor 清单(offset / caller / 开聊天1次读时出现次数)** —— 优先从"每次恰好1次"的查(已读上报通常 once-per-open):
> | desc offset | caller(+off) | 次数 | 备注 |
> |---|---|---|---|
> | 0x6a29078 | 0x5aed308 (read簇) | 1 | **已排除**(单拦命中但对方仍已读) |
> | 0x6a29b98 | 0x5ab9164 | 1 | 候选 |
> | 0x6a29ab8 | 0x5af66a4 | 1 | 候选 |
> | 0x6a29a98 | 0x5af6430 | 1 | 候选 |
> | 0x6a29998 | 0x5af4764 | 1 | 候选(caller 在静态8-caller里) |
> | 0x6a295b8 | 0x5ba9f74 | 1 | 候选 |
> | 0x6a29b18 | 0x5aab9ac | 5~11 | 高频(疑按消息拉取) |
> | 0x6a29af8 | 0x5aaa7ec | 5~11 | 高频 |
> | 0x6a29ff8 | 0x5b34b78 | 4~8 | 高频 |
> | 0x6a2a0d8 | 0x5b7ae8c | 3~6 | |
> | 0x6a29fd8 | 0x5b57dac | 3~6 | |
> | 0x6a2a1b8 | 0x407e5e4 | 4 | |
> | 0x6a29c98 | 0x5a84ca8 | 4 | |
> | 0x6a29c78 | 0x5a88670 | 4 | |
> | 0x6a29c58 | 0x59cef98 | 4 | |
> | 0x6a26a38 | 0x5b7b368 | 4 | |
>
> 注意:上面 5 个"1次"候选里**有一个是收消息/拉取必需命令**(批量拦时弄坏了接收)——单拦到它时会"收不到消息",
> 那个排除即可,继续下一个。若 5 个都不是载体,再查高频那批(已读可能按消息逐条上报)。
> 关键偏移恒定(base 随 ASLR 变,offset 不变):`sub_59AFFEC=0x59affec`,descriptor 表在 `0x6a29xxx`/`0x6a2axxx`。
>
> **本会话(/25)已排除候选汇总(都"命中/存在但非 1:1 开聊天已读载体"):**
>   put_read 全路(gen_put_packets 0x5aec998 不触发 / transmit 0x5aef148=埋点 / sub_5AEDB20 发送派发 neuter 无效)、
>   auto_open_v2(门控关)、sdk_auto_open_translate(门控关)、send 原语 0x5b6ef5c/0x5b6fcb8(仅门控块用)。
>   **剩:handler 无条件段 + poll future 的无条件发送。**
>
> **替代路线(不必找到精确函数)**:SSL_write hook 已证可用且反篡改安全(hook libttboringssl)。虽帧是 HTTP/2+应用加密,
>   但若做**精确读窗口相关**(多次开聊天,找每次都出现、大小一致的帧)认出已读帧,可在 SSL_write **直接 drop**
>   (不需知道是哪个 liblark 函数)。坑:加密帧难区分(`82 fa` 心跳也是 128B)、drop TLS 记录可能打乱流。属兜底。
>   1. 最可能是 `im.chat.auto_open_v2`（§3.1 注"携带已读位置"）。doc §3.3#1 说它的门控 0x5774c64 返回 0、
>      "强制返回0没改变任何事"——但那只说明*那个门控*不是发送开关，**不等于 auto_open 不发**。需重测：
>      trace auto_open 载入点 `0x59dc03c` + 读路径 poll `0x5aed350`(2 caller 0x5971d74/0x59dc458) 入口，
>      看开聊天时真正跑哪条、它把"已读位置"塞进哪个 cmd。
>   2. 找**真正的**统一序列化/打包点（不是 0x6111d8c）：在读时它会被各 cmd 调到；从那里读 cmd 号即可认出已读命令。
>   3. 或回到数据源：sqlite `read_position`/`message_read_states` 表写入→上报的那一跳（Path C，下条）。
> - **C = sqlite read_position 侧**：复用防撤回 sqlite 基建，找上报前读取的 `read_position`/`read_state`
>   表（实测 schema 有 `message_read_states`、`read_position`、`show_read_state_type` 等），从"读取已读位置去上报"
>   那一侧动手（数据源头法）。
> - ✗ **已废**：拦 transmit / packer(0x6111d8c) / gen_put_packets / SSL_write / sendto（要么够不到异步 send，
>   要么压根不在开聊天的已读路径上）。

下方为原始 §8（保留备查）：

1. **找 Feishu 内部「已读回执」开关 flag**（最可能一击毙命）。auto_open_v2 的门控 `0x5774c64` 证明 Feishu 有命令级特性门控。若已读回执也有类似 flag（或用户设置项的内存映像），翻一下即全局关，最干净。思路：搜 `read_receipt`/`receipt_enable`/`show_read` 等字符串 + 追其读取处。
2. **确认 `0x58da644`（或同族）是不是「通用提交入队」**。若被多命令共用且能从请求结构里读出 cmd 标识 → 按 cmd 过滤拦截，一劳永逸。需先搞清请求结构布局。
3. **复用防撤回的 sqlite hook 基建**（本项目 `native/jni/antirecall.cpp` 已有 sqlite3_step inline hook）：找本地 `read_position`/`read_state` 表，看上报是否读取它；从「读取已读位置去上报」那一侧重拦。
4. **网络层签名法**（脆，兜底）：读时抓 put_read 的 wire 字节，用 chat_id 作特征识别，再按特征拦 sendto/SSL_write。
5. **IDA 反编译编排函数**理解流程：headless `py_eval` 在 idalib **不可用**；可 (a) 用 IDA GUI 打开 7.69.6 的 liblark.so 跑分析（慢，101MB），或 (b) 在 7.52.4 i64 上用 MCP `decompile`/`xrefs_to`（需先定位 7.52.4 对应函数，命令名锚点法）。

---

## 9. 文件索引

- 交本文档：`已读不回执-逆向进度.md`（本文件）
- 脚本归档：`stealth-read/scripts/`（见 §6）
- 工作目录脚本原档：`<本地路径>\*.js` / `drive_read.py`、日志 `read_obs.log`
- 防撤回姊妹篇：`防撤回-逆向方法论手册.md`
- 记忆（会话级速查）：`stealth-read-progress`、`adb-and-test-device`

---

## 10. frida-attach 动态实验结论

rig：florida 隐身 server（`/data/local/tmp/dmtkd2`）`:8899` + 客户端 frida 17.6.2（`/tmp/frida1762`）
+ **ATTACH**（非 spawn）到 lark 主进程。driver=`stealth-read/scripts/attach_drive.py <hunt.js>`，
日志 `attach_drive.log`。**可靠触发**：sqlite `UPDATE chats SET read_position` / `messages SET me_read`。

### 10.1 逐一证伪
| 实验 | 做法（脚本） | 结果 | 结论 |
|---|---|---|---|
| hunt2 | hook `sub_59AFFEC` 记 caller，关联读窗口 | idle 时 sub_59AFFEC 全静默；读时一串 caller。`0x5aed308`(=`sub_5AED220`) 在 read_position 后**唯一**出现一次 | 看似命中 |
| hunt3 | `Interceptor.replace sub_5AED220 → return 0` | **仍已读**，且该函数本次读根本没被调用（BLOCKED=0） | `sub_5AED220`(read_message/logic.rs, descriptor `off_6A29078`) **不是**回执载体；frontier 路再次出局 |
| hunt5 | 纯 sqlite，读窗口内记所有写库 SQL | 发现同步引擎表：`index_wal(table_identifier,event_key,status,item_id)`、`pipeline(pipe_type,change_list_*,local_sid,server_sid)`；读标记分两路：`messages.me_read`(本地显示已读) 与 `chats.read_position`(游标) | 已读走**通用 entity-sync 管线**，非专用回执命令 |
| hunt6 | 纯 sqlite，`UPDATE chats SET read_position` 返回 SQLITE_DONE(101) 跳过执行 | **仍已读**；且消息列表不刷新（read_position 还驱动 feed） | read_position 写库**不是**回执触发点；误伤 feed |

### 10.2 硬结论（本轮最重要）
**回执是「内存态驱动」，不是「读库驱动」。** App 在内存里判定"读到此"后并行做两件独立事：
(a) 写本地 DB（me_read/read_position）；(b) 把读状态入队上传。**拦 (a) 不影响 (b)** → 对方照常已读。
本地读标记与回执上传**共享同一内存信号**，在 DB 层不可分离。唯一切点 = **网络上传那一端**（保留本地写、丢上传）。
上传走通用 entity-sync 管线（`index_wal`→`pipeline`），**疑似不走 frontier(sub_59AFFEC)**（hunt3 再证）。

### 10.3 安全边界（实测）
- ✗ **hook `libttboringssl!SSL_write`**：在活动 TLS 热路径插桩（即便只读+backtrace）会 stall 连接，收发全卡，detach 也救不回，**必须 force-stop 重启 lark**。废弃。
- ✓ hook libsqlcipher `sqlite3_step`（attach 或 replace 返回 101）安全可逆。
- ✓ `Interceptor.replace` liblark .text 函数安全（hunt3）。
- 运维坑：设备 transient 断连后 **`adb forward tcp:8899` 会丢**，driver 报 `ServerNotRunningError`，需重建 forward；florida server(dmtkd2) 自身常驻不掉。

### 10.4 下一步候选（待定方向）
1. **定位 entity-sync 上传的网络 egress**：怀疑读状态 sync 走 HTTP/cronet（libsscronet）而非 frontier TLS，
   这能解释"全拦 frontier 仍已读"。需确认传输层，再在 liblark 的 sync-drain（pipeline 出队→序列化→发送）处按
   entity 类型/字段过滤掉 read 相关变更。
2. **重验 frontier**：hunt2 的"读窗口" caller 还有一批 chat-open 期的发送（0x5a88670/0x5aaa7ec/0x5b57dac/…）未逐一排除；
   回执可能在 chat-open 时发、而非 read_position 时。可用 frida 动态对这批 caller 单丢验证（保留心跳 caller 防断连）。
3. me_read/message_read_time 侧的上传 enqueue 点（内存态）回溯。

### 10.5 ★突破: 时间门控丢弃 frontier 终端入队 (hunt9)
**做法**：`Interceptor.replace sub_649FE5C`；触发 `UPDATE chats SET read_position` 后开 800ms 窗口，
窗口内丢弃所有 frontier 入队(return 0，调用者 sub_64A0364 忽略返回值，安全)。chat-open 拉消息发生在
read_position 写库**之前**，故不受影响。
**实测结果**：
- A 能收到消息 ✓ / A 能打开聊天 ✓ / 发收正常 ✓
- **B 显示未读 ✓**（回执成功拦截！frontier 确实是回执载体，但需在"读后窗口"丢才不破坏 chat-open）
- ✗ 副作用：A 自己消息列表未读红点数**不消退**。

**根因**：飞书中"我已读(清自己红点, 多端同步)"与"回执(告诉对方)"是**同一个 read_position 上传**。
拦上传 → 对方未读(要的) + 自己红点不清(不要的)。二者服务器侧同源, 在网络层不可分。

### 10.6 待解: 本地红点
要达成"对方未读 + 自己红点清", 需在拦上传的同时**本地伪造红点清零**(不依赖服务器 ACK):
- 红点非由本地 read_position/me_read 驱动(这俩已写库但红点仍不清) → 红点来自服务器同步的 feed 未读数
  (`chats.unread_count` / `feed_channel` / `feed_previews`, 见 hunt5)。
- 候选: 读后用 sqlite hook 本地强制 `unread_count=0` + 刷 feed, 复刻服务器 push 的效果。需进一步 recon
  红点数到底读哪张表/字段, 以及如何触发 UI 刷新。

### 10.7 ★★ 解法确定: 内容法丢弃读报告 (hunt12→hunt14, 已两账号验证)
**关键发现**:
1. 回执有【立即 + 周期】两条: 停留聊天页 ≥30s 会触发周期读位置上报, 逃出任何时间窗 → 时间门控(hunt9)漏它。
2. frontier 终端入队 `sub_649FE5C` 的 payload 里 **chat_id 明文可见**(hunt12): a4[1] 指向的缓冲含之。
3. 读报告把 chat_id 编成 **ASCII 十进制字符串**塞进 protobuf(`0a 13 "7000000000000000000" 10 01 18 02`),
   而其它命令(发消息/遥测)多用 **二进制小端**(cd0b40d7b7fa126a) → ASCII 形是读报告的稳定签名。
**解法(hunt14)**: `Interceptor.replace sub_649FE5C`; 维护"最近读过 chat"的 ASCII-id 集合(读 read_position 时加,
活跃 5min); 每次发送扫 payload(a4[1]缓冲256B + a4 64B), 含活跃 chat 的 ASCII-id 就丢(return 0, 调用方忽略返回值)。
**实测**: A 收发正常 + B 停留 35s 仍未读(立即 a3=6 与周期 a3=10 读报告均被丢)。✓✓
**注**: chatId 提取的正则要收紧为 WHERE `chats`.`id`=(只取聊天 id, 别误收 SET 里的 last_message_id)。

### 10.8 红点解法(进行中): feed_channel.new_message_count 本地重算
红点数 = `feed_channel.new_message_count`(REPLACE 第10列)。验证: `new_message_count == last_message_position - read_position`
(hunt11 实测 149-148=1)。服务器因回执被丢而 read_position 永久落后 → 一直回推非零 count → 红点不消。
**修法**: 本地追踪每 chat 的 read_position 与 last_message_position(均从 chats UPDATE 取); 拦 feed_channel REPLACE,
用 `sqlite3_bind_int64(stmt, 10, max(0, last_message_position - read_position))` 重绑第10个参数 → 红点反映本地真实已读
(读完=0, 新消息照常+1, 不误伤通知)。这是纯本地写, 不影响网络。

### 10.9 ★★★ 完整方案验证通过 (hunt15, 两账号实测全绿)
脚本 `stealth-read/scripts/hunt15.js`(frida)。实测: A 收发正常 + A 本地红点清 + B 停 30s 仍未读。
**两部分**:
- ① 回执: `Interceptor.replace sub_649FE5C`, payload(a4[1]缓冲)含活跃 chat 的 ASCII-id 即丢(return 0)。通杀立即+周期。
- ② 红点: 拦 `REPLACE INTO feed_channel`, `sqlite3_bind_int64(stmt,10, max(0,lmp-rp))` 重算 new_message_count。
  实测红点主要靠 App 本地乐观写自清(读完即0); 重算逻辑作为防御兜服务器回推。
**运行依赖**: florida server(dmtkd2:8899) + frida 17.6.2 客户端 + attach。每次 lark 重启需重 attach。
**待办**: 移植到原生 LSPosed 模块(antirecall.cpp)以免依赖 frida rig —— sub_649FE5C 在 liblark .text(可内联 hook,
安全区), sqlite3_step 已有 hook 基建。需在 C 里做 SQL 串解析 + 每 chat 的 rp/lmp 追踪 + ASCII-id 集合。

### 10.10 原生移植受阻: liblark .text inline hook 不可靠
把 hunt15 移植进 antirecall.cpp(sub_649FE5C 内容法丢回执 + feed_channel 红点重算 + sqlite chat 追踪),
编译/安装/加载均 OK, 日志确认两 hook 都 armed。但**实测对方仍已读**, 表现反复无常:
- sqlite hook(libsqlcipher, 非反篡改区): 稳定, TRACK/红点重算正常。
- liblark .text hook(sub_649FE5C): 时灵时不灵 —— 启动后首次读有 drop(但对方仍已读), 几分钟后读 0 drop。
- DROP-ALL 验证(decide 恒返回1): 开缓存聊天/发消息**不触发** sub_649FE5C; 读新消息时有时触发有时不。
- 加"周期重打 hook"维护线程(每800ms比对入口字节)防反篡改还原: 30s idle 0 次还原; 但读时仍 0 drop/0 TRACK
  (logcat 在设备频繁瞬断下也难取证)。
**对比**: 同一函数 frida `Interceptor.replace`(每次 attach 新鲜注入、立即测)稳定拦住=未读; 原生 A64InlineHook
作常驻 hook 却不稳。根因未完全坐实(疑: 反篡改还原/A64 与 frida 语义差异/sub_649FE5C 非唯一路径/lark 状态)。
**结论**: 原生移植暂搁置。可靠交付 = frida hunt15(已多次双账号验证)。原生模块保持纯防撤回。

### 10.11 原生攻坚铁证 ( 深夜)
逐项验证(文件日志 /data/data/com.ss.android.lark/files/sr_diag.log 取证, 避开 logcat 瞬断丢失):
- **hook 安装正确**: 读 /proc/pid/mem @base+0x649fe5c = `NOP;LDR x17;BR x17;<my_649>` = A64 分支确在。
- **neuter 有效(铁证)**: g_drop_all_test=1 丢光 sub_649FE5C → **进不去 APP**(登录/同步全断), 同 frida hunt7。
  ⇒ A64 "不调原函数直接返回" 确实阻断了原函数 (推翻"neuter 无效"猜想)。
- **失败根因 = 匹配/时序, 非 hook**: 真回执引用的是"被标记已读的 message_id"(=`UPDATE messages SET me_read WHERE id IN(...)`
  里的 id, 实测如 7000000000000000000), **非** chat_id; 且编码时而 ASCII(`0a13`+19数字) 时而二进制小端;
  还可能在 sqlite me_read 写库**之前**就发出(时序竞争)。
- 已实现 watch 集(chat_id + me_read 的 IN 列表 message_id, 双编码 ASCII+LE 匹配): 单次读丢 23 个 send 仍漏真回执
  (nactive 仅 2, 时序/编码没全覆盖)。
**真正的不变量 = frontier 命令号**(UPDATE_MESSAGES_ME_READ=1021 / CREATE_CHAT_LAST_READ_POSITION=1067 / 2224/2234/2263)。
但 7.69.6 实测读回执**不经 Java Sdk.invoke**(async, installAntiRead 早注明不可达); 命令号在 frontier 帧内的位置待定位。
**现状**: anti-recall(sqlite)稳; 防已读 native 因回执 id 编码/时序复杂未稳; frida hunt15 在其测试期可靠(但疑随 app 行为漂移)。

### 10.12 最终结论: 当前 app 版回执无稳定内容判据
用 frida 快速迭代诊断(hunt16/hunt17), 在【关掉 native stealth、隔离】条件下逐一证伪:
- **frida 旧脚本(hunt15)现已失效**: 同一台 app 重跑, 单次读只丢 1 个 chat_id-ASCII send, 对方仍已读
  ⇒ app 行为漂移, chat_id 判据失效(并非 native 移植的锅; frida/native 现表现一致)。
- **chat_id(LE+ASCII)**: 一次读出现在很多 send 里(多为 LE), native 全丢(23个)仍已读 ⇒ 回执不(稳定)含 chat_id。
- **me_read message_id**: 多数 send 在 me_read 写库前发出(时序竞争); 且追踪后也无 send 含该 id。
- **ASCII 读报告签名 0a13+19数字**: 时有时无(hunt16 有2个, hunt17 同样操作 0 个) ⇒ 不稳定。
- 回执实际引用的 id 飘忽(实测 7000000000000000000 / 7000000000000000000 / 7000000000000000000, 均非 chat_id/me_read id)。
**唯一不变量 = frontier 命令号(1021 等)**, 但不走 Java(async)、且不在可取的 q1/a4 payload 里(payload 前段是指针+小整数三元组
(07/03/04 等)+16字节hash+常量 cd8c01e51c08a069, 命令号位置未定位)。
**判定**: hook 机制有效(drop-all 能让 APP 进不去=已证), 但"认准回执"在此 app 版无可靠通用判据。防已读暂时受阻于 app 侧改动。
**保留**: frida 脚本 hunt2..hunt17 + native scaffolding(g_stealth_read 开关) 备查; 日常驱动 = 纯防撤回(稳)。
**续攻可能方向**: (a)定位 frontier 命令号在帧/节点(a1[8]或descriptor a4[2]对象字段)中的位置, 按命令号丢; (b)hook 读报告
Rust 构造函数族(sub_5AED220 等, 需补全成员); (c)接受 PC 常连, 用 frida + 更智能判据。

### 10.13 "为什么之前能成功" —— 回执编码漂移
同一份 frida 代码、同一个聊天, 今天早些时候(§10.7 hunt14/15)稳定让对方未读, 当晚重跑却已读。代码未改, **是 app 行为漂移**:
- 早些时候: 回执**带 chat_id(ASCII 形)** → frida 按 chat_id 一拦即中(DROP a3=6+a3=10 一串), 对方稳定未读。
- 当晚: 回执**改引用 message_id**(实测 7613.../7655...), chat_id 即便出现也多是 feed 同步等别的命令在用;
  frida 按 chat_id 只丢到 1 个边缘 send, 真回执溜过 → 已读。
即**读上报从"按聊天位置(带 chat_id)"漂移成"按单条消息(带 message_id)"**(疑对应命令 1067→1021 切换)。
**最可能诱因**(按可能性): ①服务器侧 feature_gating / A-B 开关当天翻动; ②当天为测试 force-stop 重启 lark 三四十次,
频繁异常重连致协议协商进不同同步模式(或触发风控降级); ③随读位置/消息数的状态依赖。
**待验**: 彻底重启设备+干净启动 lark 再跑 hunt15 —— 恢复未读=累积状态/协商(脆弱,需同时覆盖 message_id 才稳);
仍已读=服务器/版本永久改动, chat_id 判据作废。

### 10.14 重启验证: 漂移是持久性改动 ( 晚)
用户重启设备后重跑 frida hunt15(chat_id 判据): **仍 0 drop、对方已读**。
⇒ 排除"累积状态/协议协商(重启可重置)"假设; 确认**持久性改动**(服务器侧 feature gate / app 版本 / 风控降级, 重启带不回)。
chat_id 判据被 app 侧永久改没, frida 旧脚本作废(非脆弱, 是真失效)。防已读需重新逆向当前回执机制(命令号路线)。

### 10.15 命令号/类别路线结论: 回执在 sub_649FE5C 无法隔离 ( 晚)
按用户要求啃"命令号"路线, 用 frida 迭代:
- a4[0] 是每命令类型的【堆】descriptor(会话内稳定, 跨会话变), a3↔a4[0] 本次 1:1, 读窗口共 4 类: a3=2/6/10/15(generic
  通道 vt+0x578a7ac) + 1 独立类(vt+0x39f0608, 不同 channel)。
- 命令号(1021 等)的 protobuf varint **不在** q1/a4 缓冲里(hunt18 全 0); a4[0] 是堆地址(无静态 xref)。
- **迭代丢弃实测**(逐类全程丢): a3=6→消息延迟5s+列表不刷+对方已读; a3=2→同; 独立类 vt+0x39f0608→同(延迟+列表不刷+已读)。
  ⇒ **每一类丢了都只扰乱消息投递, 回执照样出门**。
**硬判断**: 当前 app 版回执不再"干净走 sub_649FE5C 某一类" —— 要么挪到别的出口/函数, 要么与消息投递纠缠、靠任意可用通道重发
(丢一类换一类, 5s 延迟=重试)。命令号/类别路线在 sub_649FE5C 这一层**无法隔离回执**。
**要复活防已读**: 需重新做一轮广度逆向, 定位漂移后读状态的【新出口】(可能是另一函数 / HTTP-cronet / 与消息流合并), 工作量大。

### 10.16 破时序尝试(接收即追 message_id) + 复盘定论 ( 深夜)
**严格复现昨天成功脚本 hunt14**(frida+florida+attach, 宽松正则追 chat_id+last_message_id):
- 昨天: 丢 8 个带 chat_id 的发送 → 未读(日志 bdxca4114/bkt0eua3s 佐证)。
- 今天热会话/重启后/force-stop全新会话: 均丢 0~1 个 → **已读**。⇒ 回执从"带 chat_id"永久漂移成"带 message_id"。
- 验证排除: frida 无需 force-stop(attach 即可); 重启/新会话都救不回 ⇒ 持久性(服务器/版本)改动, 非会话状态。
**破时序修法移植 native**: 消息入库(`REPLACE INTO messages`)即提前 watch_add(message_id), 远早于读 → 发出时已在监视;
me_read IN列表 + chat_id 兜底; ASCII+小端双编码在 sub_649FE5C 匹配丢弃。
**实测**: 单次读 **丢了 50 个**(确实匹配到大量 message_id), 但对方**仍已读**。
**最终定论**: 当前 app 版回执在 sub_649FE5C 层**无法隔离** —— chat_id / message_id(双编码) / 命令类(a3) / descriptor(vtable)
全试过, 丢 50 个仍漏; 而 drop-all 才会让 app 登录失败(证明该函数承载关键流量但回执对部分丢弃免疫)。
⇒ 回执要么已挪到别的出口/函数, 要么靠重发对丢弃免疫。**防已读受阻于 app 侧持久改动, 现有 sub_649FE5C 路线走不通**。
日常驱动恢复纯防撤回(g_stealth_read=0, scaffolding 全保留)。复活需重新做广度逆向定位漂移后的新出口。

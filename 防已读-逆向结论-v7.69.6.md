# 飞书「防对方已读」逆向结论与方法论留档

**目标**：让消息发送方进入聊天后**看不到「已读」回执**（防对方看到我已读），作为 [xposed-antirecall 防撤回模块](./) 的扩展需求。

**最终结论**：**防对方已读，在飞书 v7.69.6 这套全异步 Rust 架构下，用 LSPosed inline-hook 不可达。** 已穷尽五大层 + 多轮设备实测验证。防撤回插件本身在 7.69.6 正常生效。

- 环境：设备 OnePlus PJA110（已 root，LSPosed），飞书 `com.ss.android.lark` v7.52.4 → **v7.69.6**（升级）
- 分析对象：`liblark.so` arm64（113.8MB，stripped，Rust+tokio，BuildID `6c99d0d7…`）
- 模块：`~/xposed-antirecall`（native `sqlite3_step` inline hook 防撤回）
- 时间：，跨 7.52.4 与 7.69.6 两个版本

---

## 一、核心结论

### 1. 防撤回为何版本无关、一直生效
模块 hook 的是 **`sqlite3_step`，它在 liblark 里是 `UND` 导入符号**（真身在 `libsqlcipher.so`）。按 **C 符号名** hook = 跟 liblark 的偏移彻底无关，所以飞书从 7.52.4 升到 7.69.6（跨 17 个小版本）撤回拦截照样工作（实测启动即拦截 `NEUTRALIZED recall write`）。
> 教训：**选稳定的 C-ABI 存储层咽喉，不要依赖 stripped 二进制的偏移。**

### 2. 防对方已读为何不可达（架构性，非技术不到位）
已读回执 = **情形 B**：由内存态 async `read_message::logic` poll 直接发 frontier `PUT_READ_MESSAGES`（命令号 **40 = 0x28**，跨平台共享 IDL，7.52.4→7.69.6 常量不变），**与任何本地 sqlite 写完全解耦**。

本次相比 7.52.4 的关键进展是：**证明了 inline hook 本身完全稳**（过反篡改、过 ABI、过 prologue 回跳判据，8 个 hook 同时挂 app 不崩）。问题不在 hook 能力，而在**已读发送链上没有可安全中和的落点**：

| 函数类型 | 实例（7.69.6 vaddr） | no-op 为什么崩 |
|---|---|---|
| async `Future::poll` 状态机 | `send_success` `0x5aed350` | 状态字跳表 + 巨帧；跳过腐坏状态机 |
| 返回 sret 结构体 | `F_5b1994c` `0x5b1994c`、packer `0x6111d8c` | caller 读到未初始化返回结构 |

Rust「**结构体返回 + 全异步**」惯用法 → 这条链上**任何函数 no-op 都必崩**。而真正的 wire transmit：

- 拆在**多个 async poll 状态**里（发送态返回 Pending → 响应态才打 `#read send success req_data` 日志，我多次被「日志在响应端」误导）；
- 穿过 **drop-glue shim** 和 **optimized 上行路径**（`optimized_use_of_frontier_upstream` / `message.read.req_to_frontier`）；
- 常量 `0x28` 在 async 状态机里满天飞（尺寸/tag/偏移），**无法靠它定位**。

→ 静态无法可靠 identify 真 transmit；每个 surgical 候选要「改代码→装→开聊天→问对方」一整轮，命中率极低。**结论：放弃。**

---

## 二、五大层 + 多轮实测（都试过，都堵死）

| 层 | 做法 | 结果 |
|---|---|---|
| A. native 源头 no-op | hook `read_message::logic` 整段跳过 | async poll，跳过即崩网关线程 |
| B. 网络层 | libc socket / SSL_write 掐 cmd40 字节 | socket hook 卡死启动（反篡改）；SSL_write 是 app 加密后密文 |
| C. Java 层 | hook `Sdk._invoke*(cmd, bytes)` 拦 1021 | 已读不由 Java 发；`ENTER_CHAT`(18) 无 mark_read flag，无杠杆 |
| D. sqlite 稳定层 | 冻结 `me_read` / `read_position` | ✅ 能「实现未读」；❌ 挡不住对方已读（情形 B 实证） |
| E. surgical patch | 精确 drop 发送 bl + 伪造返回 | 机制可行但命中的是响应侧（`0x5aef148`），对方仍已读 |

**7.69.6 设备实测 4 轮要点**：
1. hook `0x6111d8c`（w0=cmd packet builder，naked x8 跳板）**稳定不崩** → 破了 7.52.4「async 一 hook 就崩」的错觉；但开聊天 **0 命中** → 它不是运行时读路径。
2. **bl-target 法**精确定位运行时函数：`send_success`=`0x5aed350` 开聊天命中 6 次（在读路径上）；反汇编 = async poll（状态字跳表 + `sub sp,#0x440`）。
3. **STKSCAN**（不依赖 CFI 的安全栈扫描）抓已读落库运行时栈，得 `F_5b1994c`（每读恰 1 次）等候选 → 但都是 sret 返回，no-op 崩。
4. surgical drop `0x5aef148`（读专属、返回被忽略）→ `[ANTIREAD] dropped` 每读触发、app 不崩，**但对方仍已读**（响应侧非 transmit）；追 `movz w1,0x28; bl 0x59624f4` → 全是 drop-glue，`0x28` 是巧合。

---

## 三、可复用方法论与工具（沉淀，下次碰 stripped async rust 直接用）

1. **bl-target 法精确定函数入口**：扫 `.text` 所有 `BL` 目标即为函数起点（比 PACIASP/stp-fp 启发可靠）。7.69.6 共 642091 个入口。
2. **prologue 回跳安全判据**（hook 任何函数前必跑）：函数入口后 ~0x300 内，若有分支（`b`/`b.cond`/`cbz`/`tbz`）目标落在 `[S, S+0x14]`（被 16 字节跳转 stub 覆盖的区），则 **inline hook 必 SIGILL 崩**（`fg_check@0x5b36a88` 实证）。
3. **x8(sret) 保全的 naked 汇编跳板**：C 替换函数会在调 orig 前破坏 x8 → 崩。需 naked 保 x0-x9+x30、调 logger、复原、尾跳 orig。
4. **不依赖 CFI 的安全栈扫描**替代 `_Unwind_Backtrace`：穿 async rust 无 CFI 栈帧时 `_Unwind` 崩；改读自己栈、挑落在 liblark `.text`（vaddr `0x2fd4940..0x65fb280`）的值当候选返回地址。
5. **字符串锚 → 函数定位**（无符号）：① log 串 vaddr（rodata identity 映射 paddr=vaddr）；② 解 `.rela.dyn`（off `0xbda0`，333121 条 `R_AARCH64_RELATIVE`）找 `addend==串vaddr` 的 `.data.rel.ro` fat 指针槽；③ 扫 `.text` `ADRP+ADD` 引用该槽 → 调用点。
6. **反篡改边界**：字节系反 frida 只挡**外部 frida 侦察**（enumerateModules 隐藏 liblark + 看门狗 respawn），**不挡模块内 inline hook**（模块以 app 身份 `System.load`，早过反 frida）；实测 hook 字节未被还原。
7. **device 调试坑**：① 飞书看门狗 `am force-stop` 杀不死，必须 `su -c pkill -9`；② `my_step` 诊断日志刷爆 logcat 几秒冲掉环形缓冲 → `logcat -G 32M` + 持久后台捕获；③ `adb logcat|grep` 在某些 shell 后台易被信号打断（exit 144），用真后台进程。

---

## 四、关键地址参考（v7.69.6 liblark.so，升级会变）

| 符号/含义 | vaddr | 备注 |
|---|---|---|
| `JNI_OnLoad` | `0x3198ef0` | 唯一导出 FUNC |
| `read_message::export gen_put_packets` | `0x5aec998` | 含 `movz w0,0x28; bl 0x6111d8c`；运行时**不走** |
| 统一 packet builder | `0x6111d8c` | w0=cmd，x8 sret，51 caller；非读路径 |
| `read_message::logic send_success`(async poll) | `0x5aed350` | 状态字 `[x0+0x333]` 跳表；**运行时读路径** |
| `0x5aef148` | — | send poll 内响应侧处理，drop 之对方仍已读 |
| `fg_check im.message.put_read` | `0x5b36a88` | ⚠ prologue 有回跳，hook 必崩 |
| STKSCAN 已读落库运行时帧 | `0x5b1994c` 等 | sret 返回，no-op 崩；commit 路径与 send 解耦 |
| frontier cmd `PUT_READ_MESSAGES` | **40 (0x28)** | 跨平台共享 IDL，跨版本不变 |

> 模块字符串锚（版本可移植定位起点）：`#read gen_put_packets split_chat_data`、`#read send success req_data`、`im.message.put_read`、`lark-net-infra/src/frontier/{payload,uplink}.rs`。

---

## 五、可交付状态

- ✅ **防撤回**：7.69.6 正常生效，模块已恢复**纯防撤回模式**（所有防已读探针/hook 移除，logcat 不再刷屏）。
- ⚠ **「实现未读」**（保自己红点/未读，**挡不住对方已读**）：sqlite `me_read=0` + 选择性冻结 `read_position` 在 7.69.6 仍可做，未打包成可交付版（需改成选择性/可 toggle，避免全 chat 永久未读）。
- ❌ **防对方已读**：本架构下 inline-hook 不可达，**封档**。

*详细逐项过程见项目记忆 `project_feishu_antiread_chokepoint`。*

---

## 追加:7.70.13 复核（IDA 静态 + sqlite 实测，两路再证封档）

灵感来自"桌面 Electron 版能防对方已读（JS 层清空 updateMessagesMeRead 的 messageIds 数组）"，复核安卓能否移植。结论：**不能，两条独立证据再次撞墙。**

**① IDA 静态（liblark 7.70.13, 114MB, 全分析 IDB）**
- 字符串锚点全在。经 `.data.rel.ro` 指针槽 xref 定位读发送 poll：
  - `read_message::logic send_success`（打 `#read send success`）= **`sub_5B9A318`**（旧 7.69.6=0x5aed350）。
  - `im.message.put_read` → `sub_5BE3A64`；`optimized_use_of_frontier_upstream` 命中的 `sub_5D600DC` 是**配置/AB-flag 读取函数**，非发送点。
- `sub_5B9A318` 反编译 17KB、"positive sp"、`callees` 静态为空 → 仍是 **async poll + 间接派发**；**通用 frontier 终端入队（旧 my_649@0x649fe5c）是泛型函数、命令名拼接大 blob、无独立引用**，静态不可达（当年是 frida 动态找到的）。架构与 7.69.6 一致。

**② sqlite 实测（模块 sqlite3_step hook 内加探测，抓开聊天触发已读时所有涉 me_read/read 的 SQL 模板去重）**
- 全部只有三类：`SELECT messages.*(全列)`、`UPDATE messages SET me_read=? WHERE id IN (?..)`、`REPLACE INTO messages`。
- **不存在"取要标已读 message_id 列表"的窄 SELECT**（无 `SELECT id FROM messages WHERE me_read=0` 之类）。
- → 读回执 messageId 列表**不过 sqlite**，在内存 async 层组装。桌面"清空 messageIds 数组"在安卓 sqlite 层**无对应可清对象**——对"情形 B 与 sqlite 解耦"的直接实证（连"读取列表"入口也排除）。

**净结论**：防对方已读在 7.70.13 依旧不可达（inline-hook / sqlite 两路皆封）。可做的仍只有 sqlite `me_read=0` 冻结＝保自己未读。留档新锚点 `send_success=sub_5B9A318` 作日后动态复定位起点。

### 再追加:算法助手Pro 进程内 frida-gum 动态复核（第5次证伪）
外部 frida attach 飞书被反 frida 掐（`remote frida-server: closed`）。改用**算法助手Pro 内置的 frida-gumjs（进程内、抹了 frida 特征）**注入飞书跑脚本，`Process.findModuleByName("liblark.so")` 直接拿到基址（过反 frida）。逐步探测：
- hook `send_success=base+0x5B9A318`：开未读聊天**稳定命中**（tokio 多 worker 线程），但 FUZZY 栈全是执行器上游；
- dump 其 x0/x1：是 async future 帧；其中反复出现的 `(ptr,len=9/9/3)` 三元组，裸 dump 内容是 **ASCII 字符串**（`t:strok-`、`gb-4`、`ider` 等 log metric label）→ 实锤 **send_success 是响应侧「#read send success」日志函数，参数无可改的 messageId 列表**（正是原文档「被日志在响应端误导」）；
- hook `im.message.put_read=sub_5BE3A64`：读路径上**0 命中**；
- Stalker 前向追 send_success 的 callee 找真 transmit：**飞书闪退**（逐指令插桩与 async+反篡改不兼容）。

**净结论**：真正带 messageId 的发送在拿不到的 async 发送态里；唯一可稳定 hook 的 send_success 是事后日志。桌面「清空 updateMessagesMeRead 的 messageIds 数组」在安卓**无可达落点**。至此静态IDA / sqlite实测 / 外部frida / 进程内frida-gum **四法一致**，防对方已读封档确认无误。工具留档：算法助手Pro 的进程内 frida-gum 可过飞书反 frida，是本目标动态 RE 的正确入口（脚本见 `frida/aapro_probe*.js`），但 Stalker 会闪退，只能 Interceptor + 手工内存分析。

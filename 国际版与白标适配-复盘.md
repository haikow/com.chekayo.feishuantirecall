# 国际版 Lark + 飞书白标适配 复盘

## 背景
原模块只支持国内版飞书 `com.ss.android.lark`。本次扩展到：
1. **国际版 Lark** `com.larksuite.suite`（Google Play 版，v7.72.10 真机验证）
2. **飞书二次开发/私有化白标 app**（部分车企等企业内部沟通工具）

## 关键发现：国际版/白标的类名特征
- **应用包名**：国际版 `com.larksuite.suite`；白标各家企业不同（`com.lixiang.*` / `com.seres.*` 之类）
- **内部业务类名**：**沿用国内版前缀 `com.ss.android.lark.*`**，未随应用包名改动。国际版 dex（classes25.dex）实测保留：
  - `com.ss.android.lark.setting.page.function.SettingPageFragment`
  - `com.ss.android.lark.profile.func.v3.userprofile.UserProfileActivityV3`
  - 整个 `com.ss.android.lark.profile.*` / `setting.*` / `pb.*` 模块
- **libsqlcipher.so**：同样用，`sqlite3_step` 符号一致 → native 防撤回 inline hook 直接生效，无需改 native。

> 结论：字节系国际版/国内版/白标共用同一套飞书内核代码，只有应用包名不同。这决定了适配的核心是「按应用包名分流 + 内核类/so 复用」。

## 改动清单

### 1. xposedscope 加国际版（`arrays.xml`）
```xml
<array name="xposedscope">
    <item>com.ss.android.lark</item>
    <item>com.larksuite.suite</item>
</array>
```

### 2. 包名运行时参数化（4 个 xposed_init 类）
`AntiRecall` / `FuckLarkSettings` / `ProfileCapture` / `ResignTracker`：
- `PKG` 从 `static final` 改 `static volatile`，`handleLoadPackage` 入口按 `lpparam.packageName` 锁定
- `isLarkFamily(pkg)` 放行国内版 + 国际版
- 数据/日志路径按运行时 `PKG` 拼（`/data/data/<应用包名>/files/...`）
- `Config.setFilesDir()` / `Diag.setFilesDir()` 懒初始化（`AntiRecall.startNative` + `ResignTracker.start` 调）

### 3. 白标探测兜底（`AntiRecall.isLarkApp`）
不限包名，探测飞书内核标志类（白标保留）：
- `com.bytedance.lark.sdk.Sdk`（Java→rust bridge 入口，稳定未混淆）
- `com.bytedance.lark.pb.basic.v1.Command`（pb 命令枚举）

门口逻辑：`if (!isLarkFamily(pkg) && !AntiRecall.isLarkApp(cl)) return;`
- 国内/国际版：`isLarkFamily` 命中短路，行为不变
- 白标 app：`isLarkFamily` 未中 → `isLarkApp` 探测命中 → 注入全套

### 4. native 日志路径参数化（`antirecall.cpp`）
- 加全局 `g_data_dir[256]`（默认国内版路径做 fallback）+ JNI `nativeSetDataDir(String)`
- 5 处 `fopen("/data/data/com.ss.android.lark/files/...")` 改用 `g_data_dir` 拼（`flog` / `do_export` kicked / `my_step` leave / `decide_649` sr_diag ×2）

### 5. `build.ps1` 修复 + 补全
- javac 加 `-bootclasspath "$AndroidJar"`（原 `--release 8` 不含 Android API，android.* 类全找不到）
- NDK 缺失时 fallback：复用仓库 `feishu-antirecall.apk` 里的 `libantirecall.so`（只重编 Java）
- 加编 `libresign.so`（原 build.ps1 只编 libantirecall.so，跟 build.sh 对齐）

## 编译环境（Windows）
- **JDK17** `C:\Users\zbj\Tools\jdk17\jdk-17.0.19+10` —— 解决 d8 对 JDK22 匿名内部类 NPE（`String.length null`）。JDK22 太新，旧 d8 处理匿名类 `EnclosingMethod` 属性崩溃；JDK17 是 Android 官方标配，d8 不 NPE。
- **NDK r29** `C:\Users\zbj\Tools\ndk29\android-ndk-r29` —— 国内镜像（华为云 android/repository）只到 r21；r29 走 clash 代理（`--all-proxy=http://127.0.0.1:7897`）从 `dl.google.com` 下。
- 完整编译：`build.ps1 -NdkPath "C:\Users\zbj\Tools\ndk29\android-ndk-r29" -JdkHome "C:\Users\zbj\Tools\jdk17\jdk-17.0.19+10"`
- 签名用仓库 `debug.keystore`，证书 SHA-256 `0cc1410f...ccfd` == 代码 `EXPECTED_SIG` → 自校验通过。

## 白标 app 适配步骤（用户侧）
1. 搞到目标 app 的 APK 装到手机
2. **LSPosed Manager → fuck lark 模块 → 作用域 → 勾选该 app**（关键，勾了模块才注入该进程）
3. 打开该 app → 模块探测飞书内核标志类 → 命中即注入全套（防撤回/防已读/面板/离职/资料归档）
4. 探测失败（极少数深度定制改了内核类名）→ 拉 APK `strings classes*.dex | grep com.bytedance.lark` 找新标志类，加到 `AntiRecall.isLarkApp` 的 `marks` 数组重编译

## 验证（国际版 v7.72.10 真机，com.larksuite.suite 主进程）
logcat 确认：
- `[antirecall] native lib loaded` + `ANTI-RECALL native hook installed on sqlite3_step`（防撤回核心）
- `[fucklark] 设置入口已注入`（设置面板）
- `[fucklark] ProfileCapture hooked UserProfileActivityV3.onCreate`（资料归档）
- `LarkResign: native loaded libresign.so` + `hook installed on sqlite3_prepare_v2`（离职统计）
- 签名自校验通过（TAMPER=1 正版）

国内版 `com.ss.android.lark` 原样不受影响（双包名共存）。

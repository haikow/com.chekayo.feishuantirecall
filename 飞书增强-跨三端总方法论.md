# 飞书「防撤回 + 防对方已读」跨三端总方法论

统领文档。汇总在 **安卓(LSPosed) / Windows(Electron) / Linux(Electron)** 三端实现两个功能的架构认知、
实现手法、两次"封档翻案"的教训，以及可迁移到其它 app/功能的通用方法论。

> 细节分见：安卓 `防已读-复盘-从native碰墙到Java破局.md`、`防撤回-逆向方法论手册.md`、
> `防已读-逆向结论-v7.69.6.md`；Linux `linux-desktop-unread/防撤回-复盘-封档误判到缓存法破局.md`。

---

## 0. 两个功能 = 两条相反的数据流

理解这一点，一切定位都有方向：

| 功能 | 本质 | 数据流方向 | 拦截策略 |
|---|---|---|---|
| **防对方已读** | 你读了，对方仍显示未读 | **出站**：你的已读回执往外发 | **拦出口**，清空上报的 messageIds |
| **防撤回** | 对方撤回，你仍见原文 | **入站删除**：别人发指令进来抹你本地的原文 | **提前留副本**（事后无从渲染） |

- 出站类功能（防已读）→ 找**发送出口**，在出口清空/拦截。
- 删除类功能（防撤回）→ 找**数据消失前的时点**，提前缓存；**别指望事后把被删的东西渲染出来**。

---

## 1. 平台架构地图（功能落在哪一层）

飞书是**一套跨平台核心**：桌面 = Electron（JS SDK worker + UI），安卓 = 原生（Java/Kotlin UI + native Rust liblark 传输/存储）。**同一个服务端协议命令**（如 `UPDATE_MESSAGES_ME_READ`）在不同端由不同层组装/落地。

| | 安卓 (LSPosed) | Windows (Electron) | Linux (Electron) |
|---|---|---|---|
| **防已读** | Java pb 层：hook `UpdateMessagesMeReadRequest` 构造，清 `message_ids` | JS 出口：`updateMessagesMeRead` 前注 `t.messageIds=[]` | 同 Windows |
| **防撤回** | 存储层：inline hook `sqlite3_step`，撤回 `REPLACE` 时 `id+cid` 改绑，原行不被覆盖 | 渲染缓存：`upsertPreviews` 撤回前存 localStorage，撤回时取回拼上 | 同 Windows |

**关键认知**：桌面在 JS 层可拦，是因为 JS SDK worker 是数据出入口；安卓传输/存储在 native，但**协议命令对象仍在 Java 组装**（防已读）、**数据仍落 SQLite**（防撤回）——所以安卓也有可拦的 Java/SQL 层，不必钻 native Rust。

---

## 2. 防对方已读：三端同源，只是拦的层不同

服务端命令统一叫 **`updateMessagesMeRead` / `UPDATE_MESSAGES_ME_READ`**，载荷带 `messageIds`（安卓 pb 字段 `message_ids`）。手法统一：**清空这个 id 列表** → 服务器收不到"标这些已读"，对方一直显示未读。而你本地 UI 照常显示已读（本地读状态独立）。

- **Windows/Linux**：JS SDK worker 出口 `<logger>.info("updateMessagesMeRead",{...})` 之前注入 `t.messageIds=[],`。
- **安卓**：主已读路径 `ImSdkMessageServiceImplV2.readMessageForChannel` → 构造 `com.bytedance.lark.pb.im.v1.UpdateMessagesMeReadRequest`（Square Wire pb，类名稳定）→ 交 rustclient 发出。LSPosed hook 该 pb 构造函数，清空 `message_ids`(+`fold_ids`)。实测 `max_position` 不影响回执，无需动。
- **安卓踩过的坑**：一度以为已读发送在 native async Rust（liblark），钻了 send_success / frontier crypto / Stalker 全撞墙、误判"封档"。真相：命令在 native 之前于 **Java 层**组装好。见第 4 节。

---

## 3. 防撤回：两种范式，取决于"数据在哪被抹"

### 范式 A — 存储层保原行（安卓）
撤回 = 飞书对 `messages` 表 `REPLACE INTO`（`is_recalled=1`、content 换墓碑）。hook `sqlite3_step`，在这条 REPLACE 执行前把撤回行的 `id`^哨兵 **且** `cid` 一起改绑成唯一值 → 撤回行不撞唯一索引、不覆盖原行 → 原行（完整内容）与撤回行（提示）并存。
> 7.70.x 给 `cid` 加了 UNIQUE 索引，只改 id 会因 cid 相同连带删原行——必须 **id+cid 双改绑**。

### 范式 B — 渲染层缓存（Windows / Linux）
桌面撤回**抹掉本地原文**，UI 只剩"xxx撤回了一条消息"系统提示——事后无从渲染。故**提前缓存**：
- **撤回前**：`upsertPreviews`（会话预览更新）里把 `{id, 内容}`（`digest.elements`→richText）存进 `localStorage.recalledMessageCacheList`。
- **撤回时**：系统提示渲染处按 id 取回，拼成"xxx撤回了一条消息**: 原文**"。

**为什么不能像安卓那样"保原行"**：桌面拦不到底层存储写入（在 native/worker），只能在 JS 可见的**预览到达**这个上游时点截胡。
**为什么不能"事后渲染门控"**：数据已被抹，`renderRecalledMessage` 门控置死也没内容可渲染。

---

## 4. 两次"封档"翻案 —— 本项目最贵的教训

| 翻案 | 错误前提 | 真相 | 一句话 |
|---|---|---|---|
| **安卓防已读** | "已读发送在 native async Rust" → 钻 liblark 全撞墙 | 命令在下沉 native **之前**于 **Java 层**（pb 对象）组装，可 hook | 别让"跨平台类比"锁死层次判断 |
| **Linux 防撤回** | "把被撤回的原文事后渲染出来" → 门控全无效 | 数据已被**抹**，要**撤回前提前缓存**；且盯错了渲染路径（系统提示 vs 气泡占位） | 删除型功能靠缓存，不靠事后渲染 |

**共同根因**：结论被一个**没验证的前提**污染。前提一换（换层 / 换时点），"不可达"立刻变"可做"。
**纪律**：写"封档/不可达"时必须**显式标注前提**，并问自己——"我是不是把某一层/某个时点当成了唯一的？"

---

## 5. 通用方法论（可迁移到别的 app / 功能）

1. **先侦察，再啃硬骨头**。DEX/字符串 grep 协议名（`grep UPDATE_MESSAGES_ME_READ`）、看同源跨平台补丁——这些几分钟就有方向；别一上来钻 100MB 剥符号的 native。
2. **按数据流选层**。出站功能拦 egress（发送出口）；删除型功能在"数据消失前"提前缓存。方向对了，手法都简单。
3. **锚点优先级**：稳定的**生成 pb 类名**（`com.bytedance.lark.pb.*`）> 混淆方法名；**请求构造器** > 响应回调（`send_success` 那类是响应侧，命中也没用）。
4. **同源跨平台补丁是金矿**。Electron 桌面 Windows/Linux 是同一套 JS——Windows 补丁直接告诉你"存哪、取哪"。**.NET 补丁字符串是 UTF-16，用 `strings -e l` 抠**。
5. **命中 ≠ 有用**。hook 触发要看参数是请求侧还是响应侧；**必须端到端实测**（双机/真机看对方那端），不能只看"命中了"。
6. **稳健工程**：全 `try/catch` 兜底、**fail-open**（判不准就放行，别误伤正版）、幂等、留 `.bak` 纯净基线、命中数为 0 即中止不改原文件。
7. **封档结论标前提**（见第 4 节）。

---

## 6. 工程实操速查

**安卓（LSPosed 模块 `~/xposed-antirecall`）**
- 防撤回：native `libantirecall.so` inline hook `sqlite3_step`；Java `AntiRecall.installAntiRead2` 做防已读。
- 定位命令真值：`jadx` 反 DEX，搜 `Command` 枚举 / `*Request` pb 类。
- 诊断日志开关 + 签名自校验（重打包禁用核心功能）已内置。

**Windows（`FeiShuRevokeMsgPatcher`，本仓库 `Videos/` 目录）**
- 抠注入规则：`strings -e l -n 8 Plugins/*.dll | grep -iE 'recalledMessage|upsertPreviews|updateMessagesMeRead'`。

**Linux（`linux-desktop-unread/patch_feishu.py`）**
- 认准 asar：新版加载 `messenger-next.asar`（**不是** `messenger.asar`）；`sudo lsof | grep asar` 确认。
- 一键：`sudo python3 patch_feishu.py`；重启 `pkill -9 -x feishu ; /opt/bytedance/feishu/feishu &`。
- 注入禁忌：别插在 `let{...}=this.props` 后（截断声明表→页面异常崩）；插在函数体开头/JSX 表达式内的完整语句。

---

*维护：飞书每次更新后，安卓重装模块、桌面重跑 patch 脚本。混淆名/结构变了就按本文思路重新锚定——协议 pb 名和命令字符串通常最稳。*

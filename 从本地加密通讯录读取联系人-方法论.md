# 从飞书本地「加密通讯录」读取联系人（含已离职同事）——方法论

> 本文讲清楚 fuck lark 模块「离职同事统计 / 资料归档」是**怎么从飞书本地那个加密的 `contact.db` 里读出数据**的。
> 仅供逆向学习与研究，涉及企业通讯录数据，请自行确保合规与隐私边界，风险自负。

---

## 0. 一句话核心

**不去破解 sqlcipher 的密文**（那条路走不通，见 §2），而是**在飞书自己的进程里，复用它已经用密钥打开、正在使用的那条“已解密”数据库连接**——飞书运行时早就把 `contact.db` 解密好了，我们 hook 进程内的 sqlcipher，借它的活句柄跑我们自己的 `SELECT` 即可。

> 换句话说：**加密的是磁盘文件，不是内存里的连接。** 谁有密钥谁解密——飞书有，我们就“蹭”飞书的。

---

## 1. 目标与现状

- 飞书成员数据在本地：`/data/data/com.ss.android.lark/databases/sdk_storage/<账号uid>/contact.db`
- 该库是 **SQLCipher 加密**（AES-256 + HMAC + PBKDF2 派生密钥）
- 离职同事：飞书在服务端把该成员标记 `is_resigned=1`；本地这行还在（直到飞书 `cleanCacheIfResign` 清它），资料页却被 UI 屏蔽/清空
- 我们要的：在数据被清前，把 `chatters` 表里 `is_resigned=1` 的行读出来存档

---

## 2. 为什么“破解密文”走不通（省你白费力气）

- SQLCipher 的密钥**不在磁盘上**：由 `PRAGMA key` 传入的口令经 PBKDF2（几万次迭代）+ 每库随机 salt 派生出 AES 密钥。
- 飞书的口令来自**账号 token / keystore 派生**，不落地明文；cipher 版本、KDF 迭代、page size 等参数也未知。
- 离线爆破 = 不可行。`sqlite3` 直接打开只会得到 `file is not a database`。
- ⇒ **别碰密文，碰“已解密的运行时连接”。**

---

## 3. 正解：进程内复用飞书自己的解密连接

### 3.1 两条注入路径

| 路径 | 用途 | 特点 |
|---|---|---|
| **A. frida（隐身版）** | 侦察 / 一次性追溯统计 | 快速迭代；但飞书是字节系、有反 frida，需用改名隐身的 frida-server（见 `stealth-read/` 与相关记忆），且客户端要匹配设备 Android 版本的 frida 版本 |
| **B. LSPosed native 模块** | 常驻自动采集（本项目 `resign.cpp`） | **以飞书自己身份运行、早于其反 frida 检测**，天然过检测；durable |

两条路的**内核完全一样**：hook 进程内 `libsqlcipher.so`，拿到 contact.db 的活句柄，就地查询。

### 3.2 解析 sqlcipher 符号（关键坑：不能 dlopen）

Android 上 LSPosed 模块 / 注入代码与 app 处在**不同 linker namespace**，`dlopen("libsqlcipher.so")` 拿不到 app 已加载的那个实例。做法（`resign.cpp` 的 `find_base`/`resolve`）：

1. **读 `/proc/self/maps` 找 base**：逐行找含 `libsqlcipher.so` 且 `offset==0`、开头是 ELF magic（`7f 45 4c 46`）的映射段起始地址。
2. **手工解析 ELF `.dynsym`**：从 `PT_DYNAMIC` 段读 `DT_SYMTAB`/`DT_STRTAB`/`DT_SYMENT`/`DT_STRSZ`，符号个数用 `DT_GNU_HASH`（或 `DT_HASH`）算出（`dynsym_count`）。
3. 遍历符号表按名字匹配，解出这几个函数地址：
   - `sqlite3_prepare_v2` —— 要 hook 的点
   - `sqlite3_step` / `sqlite3_column_text` / `sqlite3_finalize` —— 就地查询用
   - `sqlite3_db_filename` —— 认库用

> 为什么不 hook `sqlite3_key_v2`（开库时传密钥那下）？也可以抓句柄，但 `prepare_v2` 更稳、每条语句都回调、且能顺带就地查询。防撤回 hook 的是 `sqlite3_step`，二者不同函数、互不冲突。

### 3.3 抓到 contact.db 的活句柄

Hook `sqlite3_prepare_v2(db, sql, ...)`。**每次回调都运行在“这个 db 自己的线程”上**。在回调里：

```c
const char* fn = sqlite3_db_filename(db, "main");   // 拿这条连接对应的磁盘文件路径
if (fn && strstr(fn, "contact.db")) {
    // 这就是通讯录库的活句柄(已解密)。存下来 / 就地查询。
}
```

### 3.4 致命坑：跨线程 = SQLITE_MISUSE(21)

SQLite 连接**不能跨线程用**。如果你把抓到的句柄丢给别的线程（比如后台轮询线程）去 `prepare/step`，直接 `SQLITE_MISUSE`。
**解法：查询必须“就地”在 prepare 回调里（= DB 自己的线程上）跑。** 本项目用一个 `pending` 标志：Java 侧请求一次 dump（`nativeArmDump`），native 在**下一次 contact.db 的 prepare 回调**里就地执行查询，避免跨线程。

### 3.5 查询本身

用抓到的原始 `sqlite3_prepare_v2`（trampoline，避免递归进自己的 hook）跑：

```sql
SELECT id, name, en_us_name, alias, another_name, tenant_id, update_time, is_frozen
FROM chatters WHERE is_resigned = 1 ORDER BY update_time DESC;
```

`sqlite3_step` 遍历、`sqlite3_column_text` 取列、`sqlite3_finalize` 收尾，写成 JSON。

### 3.6 多账号（踩过的真坑）

一台设备可登**多个账号**，就有多个 `contact.db`（各自 `sdk_storage/<uid>/`）。早期 bug：`if (!g_contact_db) g_contact_db = db;` **只锁第一个**读到的库——如果第一个是个人版（0 离职），公司版那些离职同事永远查不到。
**修法（v1.3.1）**：抓**任意** contact.db 句柄；每个采集周期各句柄**各查一次**；native 侧按 `id` 去重累积成 **union**，Java 端 add-only 合并进 `resigned_all.json`。这样多账号都能覆盖、且只增不减。

---

## 4. `chatters` 表结构速查

真实列名（≠ PB 字段名，逆向时务必先 dump 建表语句，否则 `SELECT` 报错）：

| 列 | 含义 |
|---|---|
| `id` | 成员 uid（19 位） |
| `name` / `en_us_name` | 中/英文名 |
| `alias` / `another_name` | 备注/别名 |
| `tenant_id` | **租户 id = 区分哪个公司**（个人联系人与公司同事靠它分开） |
| `is_resigned` | **1 = 已离职** |
| `is_frozen` | 1 = 冻结 |
| `is_registered` | 是否注册 |
| `update_time` | 更新时间戳 |

> 相关表：`chatter_profiles`（部门/邮箱/工号等详情，但飞书对离职者会清空，故详情要趁在职时“开资料页归档”，见模块 `ProfileCapture`）。

---

## 5. 触发时机：飞书什么时候才读 contact.db

`contact.db` 不是随便就查的——**消息列表走内存缓存，不读这个库**。可靠触发 = 进飞书 **【通讯录 → 组织架构 / 我的部门】上下滑一圈**，飞书把组织成员加载进本地库、跑 `chatters` 查询，我们的 hook 随即抓到句柄并就地快照。多账号请**分别在各账号下滑一次**。

---

## 6. 反篡改边界（为什么这条路稳）

字节系的反篡改**只死盯 `liblark.so`**（改它的 `.got.plt`/代码会被 `SIGKILL`），**不管 `libsqlcipher.so`**。所以在 sqlcipher 上做 inline hook + 就地查询**全程安全、可逆**（防撤回模块也是靠这个稳定跑的）。

---

## 7. 实现落点（本仓代码）

- **native 模块**：`native/jni/resign.cpp`
  - `find_base` / `dynsym_count` / `resolve` —— namespace 无关的符号解析
  - `my_prepare` —— hook 点：抓任意 contact.db 句柄 + pending 时就地 dump
  - `doDump` —— 就地查询 `is_resigned=1`，按 id 去重并入 union，写 JSON
  - JNI：`nativeInit`（装 hook）/ `nativeArmDump`（请求一次采集）/ `nativePoll`（取结果）
- **Java 驱动**：`app/.../larkresign/ResignTracker.java`
  - 抽 `libresign.so` → `System.load` → 周期 `arm+poll` → `mergeInto`（**add-only** 合并进 `resigned_all.json`，即便飞书日后清了本地行，我们已存档）
- **一次性侦察脚本（frida）**：`stealth-read/lg/resign_check.js`（思路同上，直接 `SELECT count(*) ... WHERE is_resigned=1` 核对各账号库有无数据）

输出：`/data/data/com.ss.android.lark/files/resign_tracker/resigned_all.json`（累计、按 uid 去重）。

---

## 8. 局限与合规

- **只能读“进程内已解密”的数据**：飞书没加载/已清掉的行读不到 ⇒ 要保住未来离职的同事，得**趁其在职、数据还在库里**时采集（滑组织架构）。
- 详细资料（部门/邮箱/工号/上级）飞书对离职者会清 ⇒ 只能**向前捕获**（打开过其资料页才归档）。
- 本方法与飞书版本无关（拦的是稳定的 SQLCipher/SQLite 接口，不依赖混淆名）。
- ⚠️ **合规**：企业通讯录/同事个人信息属敏感数据。本文与模块仅供逆向学习与个人研究，请遵守所在组织制度与当地法律，勿用于侵犯他人隐私。
